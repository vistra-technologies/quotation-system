# Stage 22 worklog — Data model foundations (Partition.design v2 + configSnapshot)

## Status
- Phase: implement — B1 + B2 code DONE & merged (release/stage-22 @ a453d2f, pushed; B1: review-1/2 + preview-b1.md 21/21; B2: review-3/4 APPROVE-WITH-NITS + dryrun-b2.md). B3 in dev on feature/s22-b3-config-snapshot-schema. OPEN: B2 real `-- --write` run on dev NOT done — awaiting human go-ahead (concern: dev DB is shared with test.easeetool.com/staging, whose code predates B1's v2 readers; recommend running only once stage code is on staging). Carry-forwards: cell-id regeneration note + transom-source order (panel selectionId -> defaults -> null; amend stage-22.md decision #2 + 04-data-model.md) -> B7 docs; fix false 'v1 always had null' comments (lib/partition-design.ts ~L20, B2 script header) in B7 dev pass; review MINOR 7 + DOOR-code coupling -> backlog at B6; dryrun-b2.md wrongly says no transom rows in dev — partition 25acae09-34c2-47ec-8e09-3956f569004f (project d38e64a9-...) has one; use for round-trip gate. B1/B2 feature branches kept (human's call to delete).
- Working branch: `release/stage-22` (cut 2026-09-19 from `origin/master` @ 20431ed; master already contains all of staging — staging is an ancestor of master — so branch is current with both)
- Approved target: d:\projects\vistra\quotation-system-docs\development-cycles\stage-22\stage-22.md (+ tasks/batch-1..7)
- Profile: .engineering/stage-22/profile.md (seeded from stage-21's; developer must refresh stage-specific bits)
- Latest artifacts: .engineering/stage-22/plan.md (D-1..D-17 at top)
- GATE A decisions (human, 2026-09-19): D-1 dev-DB `migrate deploy` for B3 APPROVED (dev only, NEVER prod); D-2 scripts never run on prod, --dry-run + prod-host refusal, real dev run only with human go-ahead; D-4 a panel maps to 1 OR 2 cells (door under transom = 2 cells) — plan's lean confirmed; D-6 normalize v2->panel view once at client fetch boundaries (do not rewrite the 9 consumers); D-11 if configurator renders category, add `category` to snapshot + note in docs; D-14 clear designSubmittedAt on partition save only, log other edits in backlog; D-15 no new unit framework — use what repo already uses (Playwright runner; pure-logic checks under it); B7 seeding = direct Prisma DB inserts in test helper against dev DB; all other D-items proceed on plan's lean.
- Tier: substantial (serial) — B1→B3→B4→B5→B6→B7, B2 script written after B1

## Work items
| Batch | Status |
|---|---|
| B1 design v2 code | DONE — merged ff into release/stage-22 (547dcee); review-2 APPROVE-WITH-NITS; preview 21/21 |
| B2 migration script | code DONE, merged (a453d2f); real dev run pending human go-ahead |
| B3 snapshot schema | in progress (feature/s22-b3-config-snapshot-schema) |
| B4 snapshot capture | todo |
| B5 configurator snapshot | todo |
| B6 designSubmittedAt | todo |
| B7 tests/docs | todo |

## Activity log

- developer · Step 2 plan written -> `.engineering/stage-22/plan.md` (profile.md refreshed for Stage 22). No product code touched. Decisions/gaps for human gate listed at top of plan (D-1..D-17): headline items = feature previews skip `migrate deploy` (B3 needs out-of-band dev-DB apply), v1->v2 door/transom serializer conflict in Batch 1, v1 read/write sites outside the batch's file list (rooms.ts seed, selections.ts delete guard, ~9 client readers, stage18 e2e), no unit-test framework. Status: DONE_WITH_CONCERNS (awaiting gate).
- developer · B1 design v2 code -> branch `feature/s22-b1-design-v2-code`, HEAD 915b236, pushed, not merged. NEW: `lib/partition-design.ts` (pure; only place branching panels vs sections), `design/partition-view.ts` (toPanelViewRow(s) at fetch boundaries, D-6). CHANGED: `lib/data/partitions.ts` (v2 write, height-sum 400, v1 normalization removed, derived width), PATCH route (parseDesignPatch; `panels` body -> 400), `design-draft-context.tsx` (Save = panelsToV2; discard/save response parsed; DraftProvider takes `selections`), `design-workspace.tsx` + `room-list.tsx` (fetch points; RoomList gains required `selections` prop), `types.ts`, `rooms.ts` seed -> v2, `selections.ts` delete guard scans cells, e2e stage18 (rewritten to v2 contract) + stage21-design (GET reads `sections`). D-4 implemented (panel -> 1 or 2 cells). Verify: `npm run lint` 0 errors, `npx tsc --noEmit` clean; throwaway tsx check of panelsToV2/parseStoredDesign round trip (glass + transom door + full-height door, wall 4000) idempotent. NOT done: preview/e2e verification (no browser tool in this dispatch). No Prisma call shape changed -> by-page.sql untouched. Status: DONE_WITH_CONCERNS.
- reviewer · B1 review round 1 (915b236 vs release/stage-22) -> verdict **CHANGES-NEEDED**; 0 CRITICAL / 2 IMPORTANT / 5 MINOR. Report: `.engineering/stage-22/review-1.md`. Verified locally: `npx tsc --noEmit` clean, `npm run lint` 0 errors. No preview/e2e verification performed (code-level review only).
- developer · B1 fix round 1 (review-1.md). FIXED #1 (design-workspace.tsx: open-time auto-glaze only for panels owning a glass cell = glass, or door with transom; full-height door no longer dirty-on-open), #2 (stage21-design.spec.ts: 3-section seed -> 4 panels / 3 panels / sections length 3), #3 (parseDesignPatch rejects schemaVersion without sections), #4 (2-cell section whose bottom cell isn't a door selection now fails), #6 partial (console.error breadcrumb in Configure-open catch; user-facing toast skipped - needs a new i18n key). SKIPPED #7 (backlog note, D-14 alongside; no code). #5 recorded: cell ids regenerate on first save (-c/-t/-d from panel id), stable thereafter; Stage 23 must NOT treat cell.id as a persistent identifier (also noted in plan.md). Verify: tsc --noEmit clean, lint 0 errors. Preview/e2e still not run. Status: DONE_WITH_CONCERNS.
- reviewer · B1 review round 2 (e875fd4, fix diff 915b236→e875fd4) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 2 new MINOR (notes only) + MINOR 5/7 carry-forwards. Both round-1 IMPORTANTs verified resolved; the new two-cell door assertion traced against every in-app producer (convert seed, TOGGLE_DOOR, panelsToV2, null bottom) — cannot fire on app-written data. Report: `.engineering/stage-22/review-2.md`. Verified locally: `npx tsc --noEmit` clean, `npm run lint` 0 errors. Merge precondition: preview e2e must run green before merging to `release/stage-22`.

- devops · B1 preview verification (2026-09-19) — branch `feature/s22-b1-design-v2-code` @ e875fd4, deployment `dpl_77KA9FiQiUDWGEburyYHvo7FNpUk` (READY, 52s build), preview URL `https://quotation-system-kicjp19m2-vistra-indias-projects.vercel.app`. Health: 200 `{"status":"ok","database":"connected"}`. Routes: 196 built, all B1 targets present (partitions, partitions/[id], design, submit-design, rooms/[id]/sides). stage18.spec.ts: **14/14 PASSED** (all API-level v2 round-trip, tenancy, invariant tests green). stage21-design.spec.ts: **1 FAILED + 6 skipped**. Failure: "Configure mode draft isolation > editing a panel without Save does not persist across reload" — `expect(getByText(/4 panels/)).toBeVisible()` timed out; DOM at failure shows left-rail "Wall 3 panels" (stale API count) while configure area correctly shows P1–P4 (draft state works). Classified: **B1 regression** — left-rail partition panel counter not reactive to draft-state changes after Add Panel; root in room-list.tsx / v2 data-flow refactor. Full report: `.engineering/stage-22/preview-b1.md`. Merge gate: NOT cleared.

- developer · B1 fix round 2 (preview failure stage21-design:199). DIAGNOSIS: NOT a B1 regression — pre-existing stale assertion. Evidence: the left-rail "Wall N panels" (room-list.tsx) renders the fetched partition and is only patched post-Save (lastSavedPartition); room-list draft use is limited to pendingRoomNameEdits, identical on release/stage-22 (B1 diff only adds `selections` prop + toPanelViewRows). No other UI renders a draft panel count in Configure mode. Convert seed became 3 panels in 810b368 (Sep 16 22:41), after the test's last edit (7f8ec42, Sep 16 12:08), so `/2 panels/` was already unmatched on baseline (Stage 21 log's pass predates the seed change). Fixed test only (tests/e2e/stage21-design.spec.ts): draft isolation now asserts panel chips ([role=button][aria-selected], wall-canvas only) 3 -> 4 after Add Panel, rail still "3 panels" and no "4 panels", then after reload rail 3 + API sections length 3; Discard test uses chip count before+1 instead of `/2 panels/`. Other 5 serial tests (width-sum, tenancy, DOM labels x2, tooltip) read as not stale from code (width-sum comment "grows the wall" is outdated wording only; add-panel redistributes locked total) but remain UNVERIFIED on preview. Verify: tsc --noEmit clean, eslint on spec clean. Preview re-run needed. Status: DONE_WITH_CONCERNS.

- devops · B1 preview re-verification round 2 (2026-09-20) — branch `feature/s22-b1-design-v2-code` @ a815c26, deployment `dpl_5d78JvBTprtb51B5yHVwEJs2D1UU` (READY, 82s build, cache from 77KA9FiQiUDWGEburyYHvo7FNpUk), preview URL `https://quotation-system-lo58euz0r-vistra-indias-projects.vercel.app`. Health: 200 `{"status":"ok","database":"connected"}`. Routes: all B1 targets confirmed (design, partitions, partitions/[id], submit-design, rooms/[id]/sides). stage18.spec.ts: **14/14 PASSED** (unchanged). stage21-design.spec.ts: **15 passed / 1 failed / 5 skipped**. Test 1 "draft isolation": **PASSED** — B1 fix (a815c26) confirmed working. Test 6 "sum(panel widths) stays exact...": **FAILED** — timeout at `widthFieldValue()` (`text=WIDTH` → `xpath=following::input[1]`); classified **PRE-EXISTING TEST BUG** (not B1 regression). Root cause: configure-mode.tsx renders WIDTH as `<span>` by default, `<input>` only when `editingDimension==="width"` (after clicking pencil icon); test never activates edit mode. Evidence: configure-mode.tsx not modified in any B1 commit (`git diff 20431ed..a815c26` empty); widthFieldValue authored in 7f8ec42 (Stage 21 engineering:test); function was never exercised before this round (serial mode blocked it). Tests 7–11 (Discard, tenancy, DOM labels ×2, tooltip): skipped (serial mode). Fix needed: test-spec only — `widthFieldValue` must click "Edit width" button before calling `.inputValue()`. Merge gate: NOT cleared (spec still has failing test, but product behavior is correct). Full report: `.engineering/stage-22/preview-b1.md` (round 2).

- developer · B1 fix round 3 (stale Stage 21 spec audit, tests/e2e/stage21-design.spec.ts only; no product code touched). Audited every test from width-sum to end against current components. FIXED: (6 width-sum) widthFieldValue -> reads the read-only span next to "Width" (input only exists after pencil click; helper only reads); wall now 2400 wide because Split is disabled <406mm (1200 wall -> 300mm panels after Add Panel); Split/Unite waits are expect(chip count) not sleeps; multi-select rewritten (selectAllPanels: Configure opens with P1 pre-selected and a plain click on the sole-selected chip DESELECTS it, so old flow selected only P2..Pn); Save waits for disabled "Saved" button; stale "grows the wall 1810" comment. (7 Discard) back button by aria-label "Back to Room Layout" (bare "←" hit could pick a different arrow); added assertion Configure toolbar is gone after Discard. (8 tenancy) traced vs api-auth/rooms/floors routes: OK, unchanged. (9 DOM label) "SAVED COMPONENTS" exact assertion was vacuous (exact is case-sensitive; DOM text "Saved Components") -> real text; "WALL DETAILS" -> "Wall Details" exact. (10 ghost-highlight) unchanged — still vacuous (count >= 0), documented in file; needs a converted partition to be real; not confirmable / not fixed. (11 tooltip) "4 sides" text no longer exists anywhere (now "N/M walls") -> wait for 5 svg polygons; card locator `.first()` matched the LEFT aside (same classes) -> filtered to the div containing svg polygon. Verify: tsc --noEmit clean, eslint on spec clean. Preview re-run needed. Status: DONE_WITH_CONCERNS.

- devops · B1 preview re-verification round 3 (2026-09-20) — branch `feature/s22-b1-design-v2-code` @ f95b4eb (test-only commit; no app source changed), deployment `dpl_8dCXmgDa6ZEDNnsxFay3g19jDNDq` (READY, 54s), preview URL `https://quotation-system-i9m0arlpi-vistra-indias-projects.vercel.app`. Health: 200 `{"status":"ok","database":"connected"}`. Routes: app source identical to round 2; all B1 routes confirmed (191+ build outputs). stage18.spec.ts: **14/14 PASSED**. stage21-design.spec.ts: **1 passed / 1 failed / 5 skipped**. Test 1 "draft isolation": PASS. Test 6 "width-sum invariant": **FAILED** at new point — `getByRole('button', { name: 'Saved' })` not visible after clicking "Save changes" (15s timeout; element never appeared). Root cause: design-draft-context.tsx glass-type guard (Stage 21 bug #13, commit 6b1938e) blocks save when any glass panel has no selectionId; `convertFirstSideToPartition` seeds glass panels with no selection; screenshot confirms warning "1 panel(s) still need a glass type assigned before saving." visible; PATCH never sent. **Classification: PRE-EXISTING TEST BUG** — guard predates B1 and was not modified by B1 (diff on 915b236 shows only panelsToV2 serializer path changed, not the guard); test never reached Save before this run. widthFieldValue fix confirmed correct (test progressed through full add/split/apply-width/make-equal/unite sequence before hitting Save). Tests 7–11: skipped (serial mode). Merge gate: NOT cleared. **ESCALATE to human/developer: fix test 6 setup (assign selection before save) or mark `test.fixme` + add to backlog.** Full report: `.engineering/stage-22/preview-b1.md` (round 3).
- (orchestrator) Round 3 preview @ f95b4eb: stage18 14/14; stage21-design 1 pass/1 fail (test 6 fails at Save: Stage-21 client guard needs glass selection; convert helper makes unglazed glass panels — pre-existing setup gap)/5 skipped. Human chose: one more fix round (assign glass in test 6 setup), then preview re-run. Fix round 4 dispatched.

- developer · B1 fix round 4 (test-only, tests/e2e/stage21-design.spec.ts; HEAD 547dcee, pushed). Test 6 setup: new helper assignGlassToPartition (creates a GLASS Selection via selections API, GETs the converted partition, PATCHes design with every cell's selectionId set) called before opening the UI, so the client Save guard passes. Reducer carries the shared selectionId through Add/Split/Unite (traced in design-draft-context.tsx), so the post-Unite panel is glazed. Post-Save assertions strengthened: schemaVersion 2, no `panels`, 1 section, section widths sum == finalWidth == 2400 == partition.widthMm, cell heights sum 2400, every cell selectionId == glass selection. Audit tests 7-11: only test 7 depends on test 6 state (projectId/partitionId; now a saved 1-panel glazed partition: Add Panel -> chips+1, enterWallConfigure filter "panel" matches "1 panel", section count compare still valid); 8-11 create own projects. No change needed. Verify: eslint spec clean, tsc --noEmit clean. Preview re-run needed. Status: DONE_WITH_CONCERNS (unverified on preview).
- (orchestrator) Fix round 4 done (547dcee, test-only: assignGlassToPartition setup for test 6 + stronger v2 post-Save asserts). Preview re-run round 4 dispatched. Merge gate NOT cleared.

- devops · B1 preview re-verification round 4 (2026-09-20) — branch `feature/s22-b1-design-v2-code` @ 547dcee (test-only; no app source changed), deployment `dpl_Bv8KwHedWdSbHTmg9kNUzhHxpcva` (READY, 57s), preview URL `https://quotation-system-q57zjlhwc-vistra-indias-projects.vercel.app`. Health: 200 `{"status":"ok","database":"connected"}`. Routes: app source identical to rounds 2/3; all B1 targets confirmed (191 build outputs). stage18.spec.ts: **14/14 PASSED**. stage21-design.spec.ts: **7/7 PASSED** — all tests reached for the first time in any round (no serial-mode skips). **MERGE GATE CLEARED. 21/21 PASS.** Full report: `.engineering/stage-22/preview-b1.md` (round 4).

- developer · B2 migration script -> branch `feature/s22-b2-design-migration-script`, pushed, not merged. NEW `prisma/migrate-design-v1-to-v2.ts` (reuses `parseStoredDesign` + `panelsToV2` + `assertSectionHeights`/`sumSectionWidths` from lib/partition-design.ts; exports pure `convertV1ToV2`/`classifyDesign`); `package.json` `migrate:design-v2`. `--dry-run`, prod-host refusal (before any client is built), idempotent (v2 skipped), malformed/invariant failures logged + not written, null transom selection written as null (counted in report). Deviations (small): cell ids are B1's `<panelId>-c/-t/-d`, not the task file's `sections_i/cells_i_j` (per plan note: ids only need uniqueness); stops-only designs (no geometry) reported as a separate "skipped" count rather than malformed. Verify: tsc clean, lint 0 errors, throwaway tsx check of convertV1ToV2 (glass + transom door w/ defaults fallback + full-height door; width mismatch rejected). NOT run against any DB (no real or dry run) — dry-run/real run left to devops per D-2. Status: DONE.

- reviewer · B2 review round 1 (0916627 vs release/stage-22, diff-b2.patch) -> verdict **CHANGES-NEEDED**; 0 CRITICAL / 5 IMPORTANT / 4 MINOR. Report: `.engineering/stage-22/review-3.md`. Headlines: `npm run migrate:design-v2 --dry-run` without `--` does a REAL write run (npm swallows the flag — verified on npm 10.9.0); prod guard is a one-host denylist, not a dev-host allowlist (and dotenv reads `.env` only, not `.env.local`); unconditional `update` can clobber a concurrent save on the shared dev DB; anomalous v1 geometry (panel heightMm drift, oversized door) silently normalized instead of reported; transom glass uses panel.selectionId over defaults.glassSelectionId — undisclosed spec deviation, and the "v1 always had null there" comment is factually wrong (pre-B1 TOGGLE_DOOR preserves the glass id). Verified locally: tsc --noEmit clean, eslint clean, pure-function behaviour exercised via tsx on synthetic docs (no DB touched); diff touches only package.json + prisma/ -> no app/lib Prisma call changed, by-page.sql correctly untouched. Process gate: task file's required dev-DB run report is still outstanding (D-2) — code approval alone cannot close B2.
- (orchestrator) HUMAN DECISION 2026-09-20 (review-3 #5): transom glass source = panel's own selectionId FIRST, then design.defaults.glassSelectionId, then null (keeps current code + B1 Save behaviour). TODO: (a) correct the false "v1 always had null there" comments in lib/partition-design.ts:~20 and the B2 script header (do in B2 fix round 2 / re-review); (b) B7 docs: amend stage-22.md decision #2 + 04-data-model.md migration rule to state this order.

- developer · B2 fix round 1 (review-3) -> `prisma/migrate-design-v1-to-v2.ts` only (lib/partition-design.ts NOT touched). #1 dry-run is now the default; real write needs `-- --write`; `--dry-run` and `npm_config_dry_run` always win. #2 fail-closed allowlist `ep-dark-term-ai0ufj4k` (strips `-pooler`; unset/unparseable/other -> abort; no escape hatch added), loads `.env.local` then `.env` (Next precedence), target endpoint printed at start and in report. #3 write is `updateMany where {id, design equals read value}`; count 0 -> "skipped, edited concurrently" list (exit code 1, re-run picks up). #4 in convertV1ToV2: glass `heightMm != wall` and door `outerFrame.h` not in 1..wall -> PartitionDesignError (validation failure, not written). MINORs taken: #7 report printed in `finally` (partial-run banner), #9 empty door.selectionId -> failure, target host reported. Skipped: #6 (parseSections not exported; would touch lib), #8 (B7 doc note), #5 untouched per human decision (comments still say "v1 always null" - TODO stays for round 2). Task file gained a "How to run" note (docs repo, uncommitted; docs tree already dirty). Verify: tsc clean, eslint clean, tsx pure checks (glass bad h / door too tall / empty door sel fail; ok cases pass; endpointOf pooler/prod/garbage/empty). NOT run against any DB. Status: DONE.
- (orchestrator) B2 fix round 1 done (a453d2f: dry-run default/--write, dev-endpoint allowlist, conditional updateMany, anomalous-geometry failures). Dispatched in parallel: review-4 (re-review, diff-b2-fix.patch) + devops READ-ONLY dry run vs dev. Comment correction for review-3 #5 (false "v1 always had null" comments in lib/partition-design.ts:~20 + script header) deferred to B7 dev pass.

- devops · B2 dry-run verification (2026-09-20) — branch `feature/s22-b2-design-migration-script` @ `a453d2f`, dev DB endpoint `ep-dark-term-ai0ufj4k` (confirmed via `.env`/`.env.local`; no password exposed). Command: `npm run migrate:design-v2` (no flags, dry-run default). Result: **34 rows with a design; 30 would convert; 4 already v2; 0 no-geometry; 0 validation failures; 0 malformed; 0 concurrent-edit skips; 0 null transom cells. Exit code 0.** Guard tests: (a) non-dev endpoint → ABORT exit 1, PrismaClient never constructed ✓; (b) `--write` without `--` separator → swallowed by npm, stayed dry-run ✓; (c) `npm --dry-run` → swallowed by npm, stayed dry-run by default (also covered by `npm_config_dry_run` env check) ✓. Full report: `.engineering/stage-22/dryrun-b2.md`. **Assessment: real run looks safe — 0 failures, all 30 v1 rows convertible cleanly, 4 v2 rows idempotent-skipped. Real run (-- --write) awaits human go-ahead per D-2.**

- reviewer · B2 review round 2 (a453d2f, fix diff `diff-b2-fix.patch` 0916627→a453d2f) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 3 MINOR. Report: `.engineering/stage-22/review-4.md`. All four dispatched IMPORTANTs verified by execution, not reading: (#1) npm 10.9.0 flag matrix re-measured — every mis-typed form (`--write` before `--`, `--dry-run`, direct tsx) falls back to DRY RUN, only `-- --write` writes; (#2) `endpointOf` fail-closed on prod/pooler-prod/unset/unparseable/localhost/near-miss, guard precedes the Prisma client import, `.env.local` > `.env` precedence correct; (#3) `design` is jsonb (Json?, no @db) and a READ-ONLY `count()` probe on the dev DB proved `design: { equals: <read value> }` matches (1), is key-order insensitive (1), and rejects a modified doc (0) — no silently-empty filter, and the failure mode would be loud anyway (per-id list + exit 1); (#4) ran the script's own pure functions over all 36 dev partitions read-only → 30 convert / 4 v2 / **0 failures, 0 malformed**, matching `dryrun-b2.md` exactly, so the new geometry assertions reject zero legitimate rows (convert seed, full-height door, door w/o outerFrame, transom door all pass). #5 behaviour confirmed unchanged (transom = panel selectionId → defaults → null); comments still deferred to B7, not re-flagged. `finally` does not mask the exit code. Diff touches no `app/**` or `lib/**` file. MINORs: dry-run report's "no door-under-transom rows" is wrong — partition `25acae09-34c2-47ec-8e09-3956f569004f` ("Lobby 1", wall 1200 / door 958 → 242mm transom, project `d38e64a9-…`) is exactly the row the task-file gate needs for the round-trip; a pre-loop error prints a report with no ABORTED banner; fractional door `outerFrame.h` still silently rounds. Verified locally: tsc --noEmit clean, eslint clean. Merge still gated on the human-approved `-- --write` run + Design-page round-trip (glass + that transom partition).

- developer · B3 snapshot schema -> branch `feature/s22-b3-config-snapshot-schema`, pushed, not merged. CHANGED: `prisma/schema.prisma` (`configSnapshot Json?` on Project), NEW hand-written `prisma/migrations/20260919000001_add_project_config_snapshot/migration.sql` (`ALTER TABLE "Project" ADD COLUMN "configSnapshot" JSONB;`), `lib/data/projects.ts` (`omit: {configSnapshot:true}` on listProjects, listProjectsPaginated, createProject create, updateProject update; getProjectById gains `options.includeConfigSnapshot` -> `omit: {configSnapshot: !include}`), `lib/data/inquiries.ts` (project create omit). Audit of all prisma.project.*: other findFirst/aggregate/count/delete/deleteMany calls use `select` or aren't row reads -> untouched; getProjectById callers (floors route, projects/[id] route) use default (omitted); B5 will pass includeConfigSnapshot. Verify: `prisma generate` ok, `tsc --noEmit` clean, lint 0 errors. NOT applied to any DB. by-page.sql: Prisma call shapes changed (omit added) -> left to B7 per instruction. Devops apply (dev only): from quotation-system/ with DATABASE_URL = ep-dark-term-ai0ufj4k (confirm host first), `npx prisma migrate deploy`; verify: `SELECT column_name,data_type,is_nullable FROM information_schema.columns WHERE table_name='Project' AND column_name='configSnapshot';` -> jsonb/YES; `SELECT count(*) FROM "Project" WHERE "configSnapshot" IS NOT NULL;` -> 0; `SELECT migration_name FROM _prisma_migrations ORDER BY finished_at DESC LIMIT 1;` -> 20260919000001_add_project_config_snapshot. Then preview: GET projects list + detail JSON have no configSnapshot key. Status: DONE.
- (orchestrator) B3 dev done (d894b6a; migration 20260919000001_add_project_config_snapshot, omit in projects.ts/inquiries.ts). Dispatched in parallel: review-5 (diff-b3.patch) + devops dev-DB `migrate deploy` (human-approved, dev ep-dark-term-ai0ufj4k ONLY) + preview check.

- reviewer · B3 review round 1 (d894b6a vs release/stage-22, diff-b3.patch) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 4 MINOR. Report: `.engineering/stage-22/review-5.md`. Verified locally: tsc --noEmit clean, lint 0 errors; migration sorts after 20260917000001 and matches `Json?` exactly (additive nullable JSONB, back-compatible with pre-B3 code on staging/master); independent grep audit of every `prisma.project.*`/`tx.project.*`/`include: { project` across app+lib+prisma+scripts (.ts and .tsx); getProjectById default omits; tenancy untouched; `omit` does not affect `data`, so B4's in-tx snapshot write is unaffected; type probe confirms B5's opt-in exposes `.configSnapshot`. MINORs: (1) `tx.project.delete` (projects.ts:534) still returns the full row - add `select: {id:true}`; (2) the "preview fails even with omit" claim is overstated - only project DELETE (and a future includeConfigSnapshot:true) break pre-migration, so a green list/detail preview is NOT proof the dev-DB migration landed; (3) `_project-fetch.ts` type + GET-route flag (plan line 146 / task acceptance) deferred to B5 - record as a B5-owned carry-forward; (4) conditional `omit` is a non-literal boolean so `.configSnapshot` types as present on default calls too.

- devops · B3 devops run (2026-09-20) — state recovery from killed prior run. Migration `20260919000001_add_project_config_snapshot` was ALREADY APPLIED by the killed run (confirmed: `prisma migrate status` = "Database schema is up to date!", 24/24 applied). NO `migrate deploy` run in this session. Dev DB endpoint: `ep-dark-term-ai0ufj4k` (both .env and .env.local, confirmed). SQL verification: `configSnapshot` = jsonb/nullable/YES; count(IS NOT NULL)=0; latest migration row = 20260919000001_add_project_config_snapshot, finished 2026-09-19T19:44:43.830Z, rolled_back_at=null. Deployment `dpl_FamA9ayK1utALkX4LpGaFfE3pBk9` READY @ d894b6a (55s build), health 200, 191 build outputs. Stray file `tests/e2e/config-snapshot-check.spec.ts`: read-only throwaway (list GET + no-configSnapshot assert), broken (wrong API path `/api/v1/projects` vs correct `/api/v1/orgs/{org}/projects`), DELETED. B3 verification: list API 20 projects no configSnapshot ✓; detail API project.* keys no configSnapshot ✓; projects list page renders ✓; project detail page renders ✓; create-project + DELETE throwaway project ✓ (5/5 targeted playwright tests). stage19 project-delete tests 2/2 PASSED (critical path: tx.project.delete works post-migration). stage19 test 3 (step-gating /design redirect) FAILED — classified PRE-EXISTING: B3 diff touches only schema.prisma + migrations + projects.ts omit + inquiries.ts omit; routing logic untouched. Full findings: `.engineering/stage-22/migrate-b3.md`. **B3 CLEAR FOR MERGE to release/stage-22.**
- (orchestrator) B3: review-5 APPROVE-WITH-NITS; dev-DB migration 20260919000001 confirmed applied (jsonb/nullable, 0 non-null; applied by the killed devops run, verified by rerun — migrate-b3.md); preview dpl_FamA9ay… @ d894b6a: no configSnapshot key in list/detail JSON, create + project DELETE pass; stage19 step-gating failure classified pre-existing (weak evidence: B3 touches no routing — recheck at test phase). Trivial nit fix (deleteProject select id) dispatched, then merge. B5-owned carry-forward: `_project-fetch.ts` type + GET-route includeConfigSnapshot flag.
- developer · B3 nit fix (review-5 MINOR 1): `tx.project.delete` in lib/data/projects.ts now passes `select: { id: true }` (return value unused). MINOR 4 left as-is (no clean few-line fix: conditional `omit` needs a generic Prisma payload overload; deferring, B5 opt-in caller can type-narrow). Verify: tsc --noEmit clean, eslint clean. DONE.
