# Stage 22 worklog — Data model foundations (Partition.design v2 + configSnapshot)

## Status
- Phase: implement — ALL BATCHES DONE. B1-B7 merged into release/stage-22 (@ 5008c42) via feature/s22-b1..b7-*. Human follow-up (backfill --project=<id> single-project filter, feature/s22-backfill-single-project) also complete: dev round1 (0269b1c) -> review-10 CHANGES-NEEDED (1 IMPORTANT: npm-swallowed --project fails open to org-wide write) -> fix round (ca607af) -> review-11 APPROVE-WITH-NITS (2 MINOR, one fixed as a docs-only follow-up commit b3ab56b correcting a false "--projects= is also caught" claim) -> merged into release/stage-22 (this commit, resolving an append-only worklog conflict from the two branches diverging after B7). Full history of B1-B7 and the backfill filter's review rounds is in review-1.md through review-12.md + the diff-*.patch/verify-*.md/dryrun-*.md files in this folder; the entries below (pre-conflict) and the tail (post-conflict, kept from both sides where non-duplicate) are thin pointers, not the source of truth.
- STANDING OPEN ITEMS (post-implement, human go-ahead required before either runs — see stage-22.md Execution Log "Standing open items" for the same list): (1) B2's real `migrate:design-v2 -- --write` HAS been run on the dev DB (verify-b2-run.md: 37 v2 / 0 v1 / 2 null, 0 invariant violations; ~30 rows updated in one burst ~2026-09-20 02:24Z; exact session unrecorded) — corrected 2026-09-20; production has NOT been run. (2) B4's real `backfill:config-snapshot -- --write` (configSnapshot backfill) on the dev DB — dry-run-verified safe (last count 565 total/563 null/0 fail), and now supports `-- --project=<id>` to run ONE project first per the human's request — human still needs to name the target project id for that first run. (3) stage19 step-gating test ('/design redirects') — confirmed pre-existing/unrelated to Stage 22 across multiple previews; flag to the tester at engineering:test, do not treat as a Stage 22 regression. (4) The new tests/e2e/stage22-data-model.spec.ts suite (8 tests) is code-complete, typechecked, and `--list`-clean but has NEVER been run against a live preview/environment (no browser tool in any implement-phase dispatch) — first real run is engineering:test's job.
- Working branch: `release/stage-22` (cut 2026-09-19 from `origin/master` @ 20431ed; master already contains all of staging — staging is an ancestor of master — so branch is current with both)
- Approved target: d:\projects\vistra\quotation-system-docs\development-cycles\stage-22\stage-22.md (+ tasks/batch-1..7)
- Profile: .engineering/stage-22/profile.md (seeded from stage-21's; developer must refresh stage-specific bits)
- Latest artifacts: .engineering/stage-22/plan.md (D-1..D-17 at top)
- GATE A decisions (human, 2026-09-19): D-1 dev-DB `migrate deploy` for B3 APPROVED (dev only, NEVER prod); D-2 scripts never run on prod, --dry-run + prod-host refusal, real dev run only with human go-ahead; D-4 a panel maps to 1 OR 2 cells (door under transom = 2 cells) — plan's lean confirmed; D-6 normalize v2->panel view once at client fetch boundaries (do not rewrite the 9 consumers); D-11 if configurator renders category, add `category` to snapshot + note in docs; D-14 clear designSubmittedAt on partition save only, log other edits in backlog; D-15 no new unit framework — use what repo already uses (Playwright runner; pure-logic checks under it); B7 seeding = direct Prisma DB inserts in test helper against dev DB; all other D-items proceed on plan's lean.
- Tier: substantial (serial) — B1→B3→B4→B5→B6→B7, B2 script written after B1
- B6 done (dev, 2026-09-20): `feature/s22-b6-design-submitted-at` @ 775bdfd, pushed. This is the last code batch — only B7 (tests/docs) remains.
- B7 done (dev, 2026-09-20): `feature/s22-b7-tests-docs`, pushed. All 8 open items from the Status block above resolved (details in the Activity log entry below). Stage 22 `engineering:implement` code-side work is now complete pending review + merge of B7 and the human go-ahead on B2/B4's real writes.

## Work items
| Batch | Status |
|---|---|
| B1 design v2 code | DONE — merged ff into release/stage-22 (547dcee); review-2 APPROVE-WITH-NITS; preview 21/21 |
| B2 migration script | DONE, merged (a453d2f); real dev run DONE (~2026-09-20 02:24Z, verify-b2-run.md; session unrecorded); production NOT run |
| B3 snapshot schema | DONE — merged ff into release/stage-22 (3c8e024) |
| B4 snapshot capture | VERIFIED — feature/s22-b4-snapshot-capture @ 273b3c3; verify-b4.md CLEAR |
| B5 configurator snapshot | DONE (dev) — feature/s22-b5-configurator-snapshot, awaiting preview verify |
| B6 designSubmittedAt | DONE (dev) — feature/s22-b6-design-submitted-at @ 775bdfd; awaiting preview verify |
| B7 tests/docs | todo |

## Activity log

- developer · Step 2 plan written -> `.engineering/stage-22/plan.md` (profile.md refreshed for Stage 22). No product code touched. Decisions/gaps for human gate listed at top of plan (D-1..D-17): headline items = feature previews skip `migrate deploy` (B3 needs out-of-band dev-DB apply), v1->v2 door/transom serializer conflict in Batch 1, v1 read/write sites outside the batch's file list (rooms.ts seed, selections.ts delete guard, ~9 client readers, stage18 e2e), no unit-test framework. Status: DONE_WITH_CONCERNS (awaiting gate).
- developer · B1 design v2 code -> branch `feature/s22-b1-design-v2-code`, HEAD 915b236, pushed, not merged. NEW: `lib/partition-design.ts` (pure; only place branching panels vs sections), `design/partition-view.ts` (toPanelViewRow(s) at fetch boundaries, D-6). CHANGED: `lib/data/partitions.ts` (v2 write, height-sum 400, v1 normalization removed, derived width), PATCH route (parseDesignPatch; `panels` body -> 400), `design-draft-context.tsx` (Save = panelsToV2; discard/save response parsed; DraftProvider takes `selections`), `design-workspace.tsx` + `room-list.tsx` (fetch points; RoomList gains required `selections` prop), `types.ts`, `rooms.ts` seed -> v2, `selections.ts` delete guard scans cells, e2e stage18 (rewritten to v2 contract) + stage21-design (GET reads `sections`). D-4 implemented (panel -> 1 or 2 cells). Verify: `npm run lint` 0 errors, `npx tsc --noEmit` clean; throwaway tsx check of panelsToV2/parseStoredDesign round trip (glass + transom door + full-height door, wall 4000) idempotent. NOT done: preview/e2e verification (no browser tool in this dispatch). No Prisma call shape changed -> by-page.sql untouched. Status: DONE_WITH_CONCERNS.
- reviewer · B1 review round 1 (915b236 vs release/stage-22) -> verdict **CHANGES-NEEDED**; 0 CRITICAL / 2 IMPORTANT / 5 MINOR. Report: `.engineering/stage-22/review-1.md`. Verified locally: `npx tsc --noEmit` clean, `npm run lint` 0 errors. No preview/e2e verification performed (code-level review only).
- developer · B1 fix round 1 (review-1.md). FIXED #1 (design-workspace.tsx: open-time auto-glaze only for panels owning a glass cell = glass, or door with transom; full-height door no longer dirty-on-open), #2 (stage21-design.spec.ts: 3-section seed -> 4 panels / 3 panels / sections length 3), #3 (parseDesignPatch rejects schemaVersion without sections), #4 (2-cell section whose bottom cell isn't a door selection now fails), #6 partial (console.error breadcrumb in Configure-open catch; user-facing toast skipped - needs a new i18n key). SKIPPED #7 (backlog note, D-14 alongside; no code). #5 recorded: cell ids regenerate on first save (-c/-t/-d from panel id), stable thereafter; Stage 23 must NOT treat cell.id as a persistent identifier (also noted in plan.md). Verify: tsc --noEmit clean, lint 0 errors. Preview/e2e still not run. Status: DONE_WITH_CONCERNS.
- reviewer · B1 review round 2 (e875fd4, fix diff 915b236→e875fd4) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 2 new MINOR (notes only) + MINOR 5/7 carry-forwards. Both round-1 IMPORTANTs verified resolved; the new two-cell door assertion traced against every in-app producer (convert seed, TOGGLE_DOOR, panelsToV2, null bottom) — cannot fire on app-written data. Report: `.engineering/stage-22/review-2.md`. Verified locally: `npx tsc --noEmit` clean, `npm run lint` 0 errors. Merge precondition: preview e2e must run green before merging to `release/stage-22`.

- devops · B1 preview verification (2026-09-19) — branch `feature/s22-b1-design-v2-code` @ e875fd4, deployment `dpl_77KA9FiQiUDWGEburyYHvo7FNpUk` (READY, 52s build), preview URL `https://quotation-system-kicjp19m2-vistra-indias-projects.vercel.app`. Health: 200 `{"status":"ok","database":"connected"}`. Routes: 196 built, all B1 targets present (partitions, partitions/[id], design, submit-design, rooms/[id]/sides). stage18.spec.ts: **14/14 PASSED** (all API-level v2 round-trip, tenancy, invariant tests green). stage21-design.spec.ts: **1 FAILED + 6 skipped**. Failure: "Configure mode draft isolation > editing a panel without Save does not persist across reload" — `expect(getByText(/4 panels/)).toBeVisible()` timed out; DOM at failure shows left-rail "Wall 3 panels" (stale API count) while configure area correctly shows P1–P4 (draft state works). Classified: **B1 regression** — left-rail partition panel counter not reactive to draft-state changes after Add Panel; root in room-list.tsx / v2 data-flow refactor. Full report: `.engineering/stage-22/preview-b1.md`. Merge gate: NOT cleared.

- developer · B1 fix round 2 (preview failure stage21-design:199). DIAGNOSIS: NOT a B1 regression — pre-existing stale assertion. Evidence: the left-rail "Wall N panels" (room-list.tsx) renders the fetched partition and is only patched post-Save (lastSavedPartition); room-list draft use is limited to pendingRoomNameEdits, identical on release/stage-22 (B1 diff only adds `selections` prop + toPanelViewRows). No other UI renders a draft panel count in Configure mode. Convert seed became 3 panels in 810b368 (Sep 16 22:41), after the test's last edit (7f8ec42, Sep 16 12:08), so `/2 panels/` was already unmatched on baseline (Stage 21 log's pass predates the seed change). Fixed test only (tests/e2e/stage21-design.spec.ts): draft isolation now asserts panel chips ([role=button][aria-selected], wall-canvas only) 3 -> 4 after Add Panel, rail still "3 panels" and no "4 panels", then after reload rail 3 + API sections length 3; Discard test uses chip count before+1 instead of `/2 panels/`. Other 5 serial tests (width-sum, tenancy, DOM labels x2, tooltip) read as not stale from code (width-sum comment "grows the wall" is outdated wording only; add-panel redistributes locked total) but remain UNVERIFIED on preview. Verify: tsc --noEmit clean, eslint on spec clean. Preview re-run needed. Status: DONE_WITH_CONCERNS.

- devops · B1 preview re-verification round 2 (2026-09-20) — branch `feature/s22-b1-design-v2-code` @ a815c26, deployment `dpl_5d78JvBTprtb51B5yHVwEJs2D1UU` (READY, 82s build, cache from 77KA9FiQiUDWGEburyYHvo7FNpUk), preview URL `https://quotation-system-lo58euz0r-vistra-indias-projects.vercel.app`. Health: 200 `{"status":"ok","database":"connected"}`. Routes: all B1 targets confirmed (design, partitions, partitions/[id], submit-design, rooms/[id]/sides). stage18.spec.ts: **14/14 PASSED** (unchanged). stage21-design.spec.ts: **15 passed / 1 failed / 5 skipped**. Test 1 "draft isolation": **PASSED** — B1 fix (a815c26) confirmed working. Test 6 "sum(panel widths) stays exact...": **FAILED** — timeout at `widthFieldValue()` (`text=WIDTH` → `xpath=following::input[1]`); classified **PRE-EXISTING TEST BUG** (not B1 regression). Root cause: configure-mode.tsx renders WIDTH as `<span>` by default, `<input>` only when `editingDimension==="width"` (after clicking pencil icon); test never activates edit mode. Evidence: configure-mode.tsx not modified in any B1 commit (`git diff 20431ed..a815c26` empty); widthFieldValue authored in 7f8ec42 (Stage 21 engineering:test); function was never exercised before this round (serial mode blocked it). Tests 7–11 (Discard, tenancy, DOM labels ×2, tooltip): skipped (serial mode). Fix needed: test-spec only — `widthFieldValue` must click "Edit width" button before calling `.inputValue()`. Merge gate: NOT cleared (spec still has failing test, but product behavior is correct). Full report: `.engineering/stage-22/preview-b1.md` (round 2).

- developer · B1 fix round 3 (stale Stage 21 spec audit, tests/e2e/stage21-design.spec.ts only; no product code touched). Audited every test from width-sum to end against current components. FIXED: (6 width-sum) widthFieldValue -> reads the read-only span next to "Width" (input only exists after pencil click; helper only reads); wall now 2400 wide because Split is disabled <406mm (1200 wall -> 300mm panels after Add Panel); Split/Unite waits are expect(chip count) not sleeps; multi-select rewritten (selectAllPanels: Configure opens with P1 pre-selected and a plain click on the sole-selected chip DESELECTS it, so old flow selected only P2..Pn); Save waits for disabled "Saved" button; stale "grows the wall 1810" comment. (7 Discard) back button by aria-label "Back to Room Layout" (bare "←" hit could pick a different arrow); added assertion Configure toolbar is gone after Discard. (8 tenancy) traced vs api-auth/rooms/floors routes: OK, unchanged. (9 DOM label) "SAVED COMPONENTS" exact assertion was vacuous (exact is case-sensitive; DOM text "Saved Components") -> real text; "WALL DETAILS" -> "Wall Details" exact. (10 ghost-highlight) unchanged — still vacuous (count >= 0), documented in file; needs a converted partition to be real; not confirmable / not fixed. (11 tooltip) "4 sides" text no longer exists anywhere (now "N/M walls") -> wait for 5 svg polygons; card locator `.first()` matched the LEFT aside (same classes) -> filtered to the div containing svg polygon. Verify: tsc --noEmit clean, eslint on spec clean. Preview re-run needed. Status: DONE_WITH_CONCERNS.

- devops · B1 preview re-verification round 3 (2026-09-20) — branch `feature/s22-b1-design-v2-code` @ f95b4eb (test-only commit; no app source changed), deployment `dpl_8dCXmgDa6ZEDNnsxFay3g19jDNDq` (READY, 54s), preview URL `https://quotation-system-i9m0arlpi-vistra-indias-projects.vercel.app`. Health: 200 `{"status":"ok","database":"connected"}`. Routes: app source identical to round 2; all B1 routes confirmed (191+ build outputs). stage18.spec.ts: **14/14 PASSED**. stage21-design.spec.ts: **1 passed / 1 failed / 5 skipped**. Test 1 "draft isolation": PASS. Test 6 "width-sum invariant": **FAILED** at new point — `getByRole('button', { name: 'Saved' })` not visible after clicking "Save changes" (15s timeout; element never appeared). Root cause: design-draft-context.tsx glass-type guard (Stage 21 bug #13, commit 6b1938e) blocks save when any glass panel has no selectionId; `convertFirstSideToPartition` seeds glass panels with no selection; screenshot confirms warning "1 panel(s) still need a glass type assigned before saving." visible; PATCH never sent. **Classification: PRE-EXISTING TEST BUG** — guard predates B1 and was not modified by B1 (diff on 915b236 shows only panelsToV2 serializer path changed, not the guard); test never reached Save before this run. widthFieldValue fix confirmed correct (test progressed through full add/split/apply-width/make-equal/unite sequence before hitting Save). Tests 7–11: skipped (serial mode). Merge gate: NOT cleared. **ESCALATE to human/developer: fix test 6 setup (assign selection before save) or mark `test.fixme` + add to backlog.** Full report: `.engineering/stage-22/preview-b1.md` (round 3).
- (orchestrator) Round 3 preview @ f95b4eb: stage18 14/14; stage21-design 1 pass/1 fail (test 6 fails at Save: Stage-21 client guard needs glass selection; convert helper makes unglazed glass panels — pre-existing setup gap)/5 skipped. Human chose: one more fix round (assign glass in test 6 setup), then preview re-run. Fix round 4 dispatched.

- developer · B1 fix round 4 (test-only, tests/e2e/stage21-design.spec.ts; HEAD 547dcee, pushed). Test 6 setup: new helper assignGlassToPartition (creates a GLASS Selection via selections API, GETs the converted partition, PATCHes design with every cell's selectionId set) called before opening the UI, so the client Save guard passes. Reducer carries the shared selectionId through Add/Split/Unite (traced in design-draft-context.tsx), so the post-Unite panel is glazed. Post-Save assertions strengthened: schemaVersion 2, no `panels`, 1 section, section widths sum == finalWidth == 2400 == partition.widthMm, cell heights sum 2400, every cell selectionId == glass selection. Audit tests 7-11: only test 7 depends on test 6 state (projectId/partitionId; now a saved 1-panel glazed partition: Add Panel -> chips+1, enterWallConfigure filter "panel" matches "1 panel", section count compare still valid); 8-11 create own projects. No change needed. Verify: eslint spec clean, tsc --noEmit clean. Preview re-run needed. Status: DONE_WITH_CONCERNS (unverified on preview).
- (orchestrator) Fix round 4 done (547dcee, test-only: assignGlassToPartition setup for test 6 + stronger v2 post-Save asserts). Preview re-run round 4 dispatched. Merge gate NOT cleared.

- devops · B1 preview re-verification round 4 (2026-09-20) — branch `feature/s22-b1-design-v2-code` @ 547dcee (test-only; no app source changed), deployment `dpl_Bv8KwHedWdSbHTmg9kNUzhHxpcva` (READY, 57s), preview URL `https://quotation-system-q57zjlhwc-vistra-indias-projects.vercel.app`. Health: 200 `{"status":"ok","database":"connected"}`. Routes: app source identical to rounds 2/3; all B1 targets confirmed (191 build outputs). stage18.spec.ts: **14/14 PASSED**. stage21-design.spec.ts: **7/7 PASSED** — all tests reached for the first time in any round (no serial-mode skips). **MERGE GATE CLEARED. 21/21 PASS.** Full report: `.engineering/stage-22/preview-b1.md` (round 4).

- developer · B2 migration script -> branch `feature/s22-b2-design-migration-script`, pushed, not merged. NEW `prisma/migrate-design-v1-to-v2.ts` (reuses `parseStoredDesign` + `panelsToV2` + `assertSectionHeights`/`sumSectionWidths` from lib/partition-design.ts; exports pure `convertV1ToV2`/`classifyDesign`); `package.json` `migrate:design-v2`. `--dry-run`, prod-host refusal (before any client is built), idempotent (v2 skipped), malformed/invariant failures logged + not written, null transom selection written as null (counted in report). Deviations (small): cell ids are B1's `<panelId>-c/-t/-d`, not the task file's `sections_i/cells_i_j` (per plan note: ids only need uniqueness); stops-only designs (no geometry) reported as a separate "skipped" count rather than malformed. Verify: tsc clean, lint 0 errors, throwaway tsx check of convertV1ToV2 (glass + transom door w/ defaults fallback + full-height door; width mismatch rejected). NOT run against any DB (no real or dry run) — dry-run/real run left to devops per D-2. Status: DONE.

- reviewer · B2 review round 1 (0916627 vs release/stage-22, diff-b2.patch) -> verdict **CHANGES-NEEDED**; 0 CRITICAL / 5 IMPORTANT / 4 MINOR. Report: `.engineering/stage-22/review-3.md`. Headlines: `npm run migrate:design-v2 --dry-run` without `--` does a REAL write run (npm swallows the flag — verified on npm 10.9.0); prod guard is a one-host denylist, not a dev-host allowlist (and dotenv reads `.env` only, not `.env.local`); unconditional `update` can clobber a concurrent save on the shared dev DB; anomalous v1 geometry (panel heightMm drift, oversized door) silently normalized instead of reported; transom glass uses panel.selectionId over defaults.glassSelectionId — undisclosed spec deviation, and the "v1 always had null there" comment is factually wrong (pre-B1 TOGGLE_DOOR preserves the glass id). Verified locally: tsc --noEmit clean, eslint clean, pure-function behaviour exercised via tsx on synthetic docs (no DB touched); diff touches only package.json + prisma/ -> no app/lib Prisma call changed, by-page.sql correctly untouched. Process gate: task file's required dev-DB run report is still outstanding (D-2) — code approval alone cannot close B2.
- (orchestrator) HUMAN DECISION 2026-09-20 (review-3 #5): transom glass source = panel's own selectionId FIRST, then design.defaults.glassSelectionId, then null (keeps current code + B1 Save behaviour). TODO: (a) correct the false "v1 always had null there" comments in lib/partition-design.ts:~20 and the B2 script header (do in B2 fix round 2 / re-review); (b) B7 docs: amend stage-22.md decision #2 + 04-data-model.md migration rule to state this order.

- developer · B2 fix round 1 (review-3) -> `prisma/migrate-design-v1-to-v2.ts` only (lib/partition-design.ts NOT touched). #1 dry-run is now the default; real write needs `-- --write`; `--dry-run` and `npm_config_dry_run` always win. #2 fail-closed allowlist `ep-dark-term-ai0ufj4k` (strips `-pooler`; unset/unparseable/other -> abort; no escape hatch added), loads `.env.local` then `.env` (Next precedence), target endpoint printed at start and in report. #3 write is `updateMany where {id, design equals read value}`; count 0 -> "skipped, edited concurrently" list (exit code 1, re-run picks up). #4 in convertV1ToV2: glass `heightMm != wall` and door `outerFrame.h` not in 1..wall -> PartitionDesignError (validation failure, not written). MINORs taken: #7 report printed in `finally` (partial-run banner), #9 empty door.selectionId -> failure, target host reported. Skipped: #6 (parseSections not exported; would touch lib), #8 (B7 doc note), #5 untouched per human decision (comments still say "v1 always null" - TODO stays for round 2). Task file gained a "How to run" note (docs repo, uncommitted; docs tree already dirty). Verify: tsc clean, eslint clean, tsx pure checks (glass bad h / door too tall / empty door sel fail; ok cases pass; endpointOf pooler/prod/garbage/empty). NOT run against any DB. Status: DONE.
- (orchestrator) B2 fix round 1 done (a453d2f: dry-run default/--write, dev-endpoint allowlist, conditional updateMany, anomalous-geometry failures). Dispatched in parallel: review-4 (re-review, diff-b2-fix.patch) + devops READ-ONLY dry run vs dev. Comment correction for review-3 #5 (false "v1 always had null" comments in lib/partition-design.ts:~20 + script header) deferred to B7 dev pass.

- devops · B2 dry-run verification (2026-09-20) — branch `feature/s22-b2-design-migration-script` @ `a453d2f`, dev DB endpoint `ep-dark-term-ai0ufj4k` (confirmed via `.env`/`.env.local`; no password exposed). Command: `npm run migrate:design-v2` (no flags, dry-run default). Result: **34 rows with a design; 30 would convert; 4 already v2; 0 no-geometry; 0 validation failures; 0 malformed; 0 concurrent-edit skips; 0 null transom cells. Exit code 0.** Guard tests: (a) non-dev endpoint → ABORT exit 1, PrismaClient never constructed ✓; (b) `--write` without `--` separator → swallowed by npm, stayed dry-run ✓; (c) `npm --dry-run` → swallowed by npm, stayed dry-run by default (also covered by `npm_config_dry_run` env check) ✓. Full report: `.engineering/stage-22/dryrun-b2.md`. **Assessment: real run looks safe — 0 failures, all 30 v1 rows convertible cleanly, 4 v2 rows idempotent-skipped. Real run (-- --write) awaits human go-ahead per D-2.**

- reviewer · B2 review round 2 (a453d2f, fix diff `diff-b2-fix.patch` 0916627→a453d2f) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 3 MINOR. Report: `.engineering/stage-22/review-4.md`. All four dispatched IMPORTANTs verified by execution, not reading: (#1) npm 10.9.0 flag matrix re-measured — every mis-typed form (`--write` before `--`, `--dry-run`, direct tsx) falls back to DRY RUN, only `-- --write` writes; (#2) `endpointOf` fail-closed on prod/pooler-prod/unset/unparseable/localhost/near-miss, guard precedes the Prisma client import, `.env.local` > `.env` precedence correct; (#3) `design` is jsonb (Json?, no @db) and a READ-ONLY `count()` probe on the dev DB proved `design: { equals: <read value> }` matches (1), is key-order insensitive (1), and rejects a modified doc (0) — no silently-empty filter, and the failure mode would be loud anyway (per-id list + exit 1); (#4) ran the script's own pure functions over all 36 dev partitions read-only → 30 convert / 4 v2 / **0 failures, 0 malformed**, matching `dryrun-b2.md` exactly, so the new geometry assertions reject zero legitimate rows (convert seed, full-height door, door w/o outerFrame, transom door all pass). #5 behaviour confirmed unchanged (transom = panel selectionId → defaults → null); comments still deferred to B7, not re-flagged. `finally` does not mask the exit code. Diff touches no `app/**` or `lib/**` file. MINORs: dry-run report's "no door-under-transom rows" is wrong — partition `25acae09-34c2-47ec-8e09-3956f569004f` ("Lobby 1", wall 1200 / door 958 → 242mm transom, project `d38e64a9-…`) is exactly the row the task-file gate needs for the round-trip; a pre-loop error prints a report with no ABORTED banner; fractional door `outerFrame.h` still silently rounds. Verified locally: tsc --noEmit clean, eslint clean. Merge still gated on the human-approved `-- --write` run + Design-page round-trip (glass + that transom partition).

- developer · B3 snapshot schema -> branch `feature/s22-b3-config-snapshot-schema`, pushed, not merged. CHANGED: `prisma/schema.prisma` (`configSnapshot Json?` on Project), NEW hand-written `prisma/migrations/20260919000001_add_project_config_snapshot/migration.sql` (`ALTER TABLE "Project" ADD COLUMN "configSnapshot" JSONB;`), `lib/data/projects.ts` (`omit: {configSnapshot:true}` on listProjects, listProjectsPaginated, createProject create, updateProject update; getProjectById gains `options.includeConfigSnapshot` -> `omit: {configSnapshot: !include}`), `lib/data/inquiries.ts` (project create omit). Audit of all prisma.project.*: other findFirst/aggregate/count/delete/deleteMany calls use `select` or aren't row reads -> untouched; getProjectById callers (floors route, projects/[id] route) use default (omitted); B5 will pass includeConfigSnapshot. Verify: `prisma generate` ok, `tsc --noEmit` clean, lint 0 errors. NOT applied to any DB. by-page.sql: Prisma call shapes changed (omit added) -> left to B7 per instruction. Devops apply (dev only): from quotation-system/ with DATABASE_URL = ep-dark-term-ai0ufj4k (confirm host first), `npx prisma migrate deploy`; verify: `SELECT column_name,data_type,is_nullable FROM information_schema.columns WHERE table_name='Project' AND column_name='configSnapshot';` -> jsonb/YES; `SELECT count(*) FROM "Project" WHERE "configSnapshot" IS NOT NULL;` -> 0; `SELECT migration_name FROM _prisma_migrations ORDER BY finished_at DESC LIMIT 1;` -> 20260919000001_add_project_config_snapshot. Then preview: GET projects list + detail JSON have no configSnapshot key. Status: DONE.
- (orchestrator) B3 dev done (d894b6a; migration 20260919000001_add_project_config_snapshot, omit in projects.ts/inquiries.ts). Dispatched in parallel: review-5 (diff-b3.patch) + devops dev-DB `migrate deploy` (human-approved, dev ep-dark-term-ai0ufj4k ONLY) + preview check.

- reviewer · B3 review round 1 (d894b6a vs release/stage-22, diff-b3.patch) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 4 MINOR. Report: `.engineering/stage-22/review-5.md`. Verified locally: tsc --noEmit clean, lint 0 errors; migration sorts after 20260917000001 and matches `Json?` exactly (additive nullable JSONB, back-compatible with pre-B3 code on staging/master); independent grep audit of every `prisma.project.*`/`tx.project.*`/`include: { project` across app+lib+prisma+scripts (.ts and .tsx); getProjectById default omits; tenancy untouched; `omit` does not affect `data`, so B4's in-tx snapshot write is unaffected; type probe confirms B5's opt-in exposes `.configSnapshot`. MINORs: (1) `tx.project.delete` (projects.ts:534) still returns the full row - add `select: {id:true}`; (2) the "preview fails even with omit" claim is overstated - only project DELETE (and a future includeConfigSnapshot:true) break pre-migration, so a green list/detail preview is NOT proof the dev-DB migration landed; (3) `_project-fetch.ts` type + GET-route flag (plan line 146 / task acceptance) deferred to B5 - record as a B5-owned carry-forward; (4) conditional `omit` is a non-literal boolean so `.configSnapshot` types as present on default calls too.

- devops · B3 devops run (2026-09-20) — state recovery from killed prior run. Migration `20260919000001_add_project_config_snapshot` was ALREADY APPLIED by the killed run (confirmed: `prisma migrate status` = "Database schema is up to date!", 24/24 applied). NO `migrate deploy` run in this session. Dev DB endpoint: `ep-dark-term-ai0ufj4k` (both .env and .env.local, confirmed). SQL verification: `configSnapshot` = jsonb/nullable/YES; count(IS NOT NULL)=0; latest migration row = 20260919000001_add_project_config_snapshot, finished 2026-09-19T19:44:43.830Z, rolled_back_at=null. Deployment `dpl_FamA9ayK1utALkX4LpGaFfE3pBk9` READY @ d894b6a (55s build), health 200, 191 build outputs. Stray file `tests/e2e/config-snapshot-check.spec.ts`: read-only throwaway (list GET + no-configSnapshot assert), broken (wrong API path `/api/v1/projects` vs correct `/api/v1/orgs/{org}/projects`), DELETED. B3 verification: list API 20 projects no configSnapshot ✓; detail API project.* keys no configSnapshot ✓; projects list page renders ✓; project detail page renders ✓; create-project + DELETE throwaway project ✓ (5/5 targeted playwright tests). stage19 project-delete tests 2/2 PASSED (critical path: tx.project.delete works post-migration). stage19 test 3 (step-gating /design redirect) FAILED — classified PRE-EXISTING: B3 diff touches only schema.prisma + migrations + projects.ts omit + inquiries.ts omit; routing logic untouched. Full findings: `.engineering/stage-22/migrate-b3.md`. **B3 CLEAR FOR MERGE to release/stage-22.**
- (orchestrator) B3: review-5 APPROVE-WITH-NITS; dev-DB migration 20260919000001 confirmed applied (jsonb/nullable, 0 non-null; applied by the killed devops run, verified by rerun — migrate-b3.md); preview dpl_FamA9ay… @ d894b6a: no configSnapshot key in list/detail JSON, create + project DELETE pass; stage19 step-gating failure classified pre-existing (weak evidence: B3 touches no routing — recheck at test phase). Trivial nit fix (deleteProject select id) dispatched, then merge. B5-owned carry-forward: `_project-fetch.ts` type + GET-route includeConfigSnapshot flag.
- developer · B3 nit fix (review-5 MINOR 1): `tx.project.delete` in lib/data/projects.ts now passes `select: { id: true }` (return value unused). MINOR 4 left as-is (no clean few-line fix: conditional `omit` needs a generic Prisma payload overload; deferring, B5 opt-in caller can type-narrow). Verify: tsc --noEmit clean, eslint clean. DONE.

- developer · B4 snapshot capture -> branch `feature/s22-b4-snapshot-capture`, HEAD 273b3c3, pushed, not merged. NEW `lib/config-snapshot.ts` (pure `buildConfigSnapshot` + `loadConfigSnapshot(db, orgId)`, structural db type so it takes a tx client; ordered `sortOrder, code`, all types active+inactive, `fieldOptionsConfig` `{}` when no org-config row, no category/FKs). CHANGED `lib/data/projects.ts` `createProject` and `lib/data/inquiries.ts` `convertInquiryToProject` (the only two `project.create` sites): snapshot loaded inside the existing interactive `$transaction` and written in the same `project.create` (D-13); B3 `omit` kept. No update path touches configSnapshot (grep: only these two creates + backfill write it). NEW `prisma/backfill-config-snapshots.ts` (B2 conventions: dry-run default, `-- --write`, fail-closed allowlist ep-dark-term-ai0ufj4k, prints target endpoint, only `configSnapshot IS NULL` via `Prisma.DbNull`, conditional `updateMany` per row in its own tx, per-row failure isolation, exit 1 on failures, report in `finally`); `endpointOf` mirrored (not shared -- avoids touching the approved B2 script). `package.json` script `backfill:config-snapshot` (singular, per dispatch; task file says plural -- B7 doc fix). D-11 OUTCOME: configurator does NOT render `category` (only declared in the row type, add-selection-form.tsx:45; no `.category` reads under projects/[projectId]) -> snapshot shape unchanged, no doc change needed. Verify: tsc --noEmit clean, lint 0 errors; tsx check of buildConfigSnapshot (inactive incl., `{}` fallback, order kept); guard check: prod endpoint -> ABORT exit 1. NOT run against any DB. by-page.sql NOT updated (B3's omit + B4's componentType/orgConfig read + project create data change all left for B7). Status: DONE.
- (orchestrator) B4 dev done (273b3c3): lib/config-snapshot.ts, createProject + convertInquiryToProject write snapshot in-tx, prisma/backfill-config-snapshots.ts (dry-run default, dev allowlist), npm backfill:config-snapshot. D-11: configurator does NOT render category -> shape unchanged. Dispatched in parallel: review-6 (diff-b4.patch) + devops preview check (two-org creation, no leak, inquiry conversion) + backfill DRY RUN only on dev.

- devops · B4 preview verification (2026-09-20) — branch feature/s22-b4-snapshot-capture @ 273b3c3, deployment dpl_ABWY3GGBsHJzkwjqd5Lf8stfyzh4 (READY, ~56s build), preview URL https://quotation-system-6pecbycus-vistra-indias-projects.vercel.app. Dev DB endpoint ep-dark-term-ai0ufj4k confirmed (both .env and .env.local). Health: 200 {"status":"ok","database":"connected"}. **Part A — Backfill dry run (2 runs):** 565 total projects, 563 NULL configSnapshot (per-org: acme-glass 453, vistra 87, nordic-walls 19, cloisons 4), 0 failures, 0 concurrent-skips. Exit 0. Prod-endpoint guard ABORT confirmed (ep-little-paper-aipm0o0i → refused before PrismaClient constructed). Mistyped --write without -- separator stays dry-run (npm swallows flag; process.argv empty → dryRun=true). **Part B — API/DB verification (6/6 Playwright tests PASSED):** T1-T5 API-level: create response / list / detail / PATCH all omit configSnapshot key; inquiry-to-project conversion also absent. T6 DB-level (execSync+tsx while projects live): all 3 throwaway projects (ACME direct, NORDIC direct, ACME converted) confirmed hasSnapshot=true, takenAt valid ISO, topKeys exactly [takenAt,componentTypes], componentTypeCount==orgTypeCount (ACME 317/317, NORDIC 3/3), tenancyOk=true, typeShapeOk=true. fieldOptionsConfig: ACME 254 empty {} + 63 populated; NORDIC 3 populated (NORDIC has config rows for all 3 types; empty-{} fallback is correct for orgs without config rows). 3 throwaway projects deleted in afterAll. **stage19.spec.ts 2/6 PASSED, test 3 FAILED:** step-gating fresh-project redirects fails identically on release/stage-22 preview (3c8e024, no B4 code) → classified PRE-EXISTING; B4 diff touches no routing/gating logic. Tests 1+2 PASSED on both previews. **Cleanup:** 4 leftover stage19 test projects (DRAFT) deleted via prisma.deleteMany. All temp scripts removed. Full report: .engineering/stage-22/verify-b4.md. **B4 CLEAR FOR MERGE.**


- reviewer · B4 review round 1 (273b3c3 vs release/stage-22, diff-b4.patch) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 5 MINOR. Report: `.engineering/stage-22/review-6.md`. Verified by execution: tsc/eslint clean; both (and only) project-create sites write the snapshot inside their existing interactive tx; no update path / no response leak (`includeConfigSnapshot` has zero callers); snapshot shape, ordering and `{}` fallback confirmed against the live dev DB with zero cross-org types in 4 orgs; npm 10.9.0 flag matrix re-measured (`-- --write` is the only write form); prod/garbage endpoint guards abort before the client; `{equals: Prisma.DbNull}` verified to match all 563 SQL-NULL rows (JsonNull matches 0); full read-only dry run exit 0, 563 would-backfill, 0 failures. D-11 CONFIRMED: configurator renders only icon(code)+name, never `category` -> decision #6 shape stands. MINORs: npm script name is `backfill:config-snapshot` but task file/plan say `...-snapshots`; backfill reloads org config per row (563 txs, measured 9.6 min, no progress output — memoize per org, only 4 orgs); script-guard block duplicated from B2 rather than shared; B5 carry-forward (snapshot stores RAW fieldsSchema/fieldOptionsConfig — must be run through parseFieldsSchema/parseFieldOptionsConfig; `[]` snapshot is legitimate); acme-glass e2e litter (317 types) makes dev snapshots ~82 KB and adds ~1s inside the create tx. Note: verify-b4.md's "post-cleanup 561/561/0" is stale — dev DB now 565/563/2; re-run the dry run right before the human-approved `-- --write`.

- developer · B5 configurator reads snapshot -> branch `feature/s22-b5-configurator-snapshot` (off release/stage-22 @ 273b3c3), HEAD 6013a93, pushed. CHANGED `app/api/v1/orgs/[orgSlug]/projects/[projectId]/route.ts` GET: now calls `getProjectById(session, projectId, { includeConfigSnapshot: true })` (B3's opt-in flag, per D-10; this route backs `fetchProjectDetail()`, the one project-detail fetch shared by every page under `/projects/[projectId]/*`, so it rides the existing payload — no new API call). CHANGED `_project-fetch.ts`: `ProjectDetail.configSnapshot: ConfigSnapshot | null` added; audited every other consumer (layout.tsx, detail/edit/summary/quotation/design pages) — none spread `project` wholesale into a client component, all destructure named fields, so the raw snapshot never reaches a client bundle except where Configuration page explicitly parses+forwards it. CHANGED `configuration/page.tsx`: `usingSnapshot = project.configSnapshot !== null`; non-null -> maps `configSnapshot.componentTypes` into the existing `ComponentTypeRow` shape (parsed via new shared parsers, see below), **no** `component-types` internalFetch issued on that path (verified: the fetch call itself moved inside the `else` branch, so listComponentTypes/its Prisma query never runs when snapshot present — satisfies the reviewer's independent-grep AC literally, not just by convention); null -> `// null-guard: snapshot absent (pre-backfill window) — reading live config` + the original live fetch, unchanged. `snapshotNotice={usingSnapshot}` passed to `AddSelectionForm`. CHANGED `add-selection-form.tsx`: `category` made optional on `ComponentTypeOption` (D-11 confirmed unused — grepped, only declared, never read); new `snapshotNotice` prop renders the exact-copy inline notice from the task file, above the 3-column grid, only when true. **Deviation from plan.md's literal wording** (not from intent): plan said "reuse `parseFieldsSchema`/`parseFieldOptionsConfig` by exporting them from `lib/data/components.ts`" — doing that literally hits the Stage-12 eslint `no-restricted-imports` ban (org-scoped `app/[orgSlug]/**` can't import `lib/data/*`), caught by `npm run lint` (1 error) on first attempt. Fixed by extracting both functions into new `lib/parse-field-config.ts` (dependency-free, same class as `lib/configurator-gating.ts` — the established pattern here, see `lib/types/field-entry.ts`'s identical precedent for the *type* extraction); `lib/data/components.ts` now imports from it instead of defining them locally (single implementation, both callers). D-12 (createSelection stays on live reads) untouched — confirmed by grep, no changes to `lib/data/selections.ts` or `actions.ts`. Verify: `npx tsc --noEmit` clean; `npm run lint` clean (0 errors; pre-existing warnings only, none in touched files) — first lint run caught the import-ban error above, fixed before commit. Preview `dpl_7QNpcV7eMCuvU2KSYu4Tiom1k4nm` (https://quotation-system-ix8f09jj5-vistra-indias-projects.vercel.app) READY (build log confirms both the page and API routes present); health 200. Live Playwright verification against that preview (temp spec, deleted after, not committed): created a fresh acme-glass project -> detail API confirms `configSnapshot` non-null with 317 componentTypes -> Configuration page shows the notice + all 317 palette tiles -> project list API response has no `configSnapshot` key (freeze doesn't leak into list payloads) -> cleaned up (DELETE, 200). Separately loaded a genuine pre-Stage-22 project (detail-confirmed `configSnapshot: null`) on the same preview -> Configuration page rendered fully (sidebar/form/saved list) with **no** notice and no crash (null-guard path). Both paths behave as designed. Status: DONE.
- (orchestrator) B5 dev done (6013a93/b1e82de): GET route passes includeConfigSnapshot; _project-fetch.ts type added; configuration/page.tsx builds palette from snapshot.componentTypes w/ null-guard fallback to live read + notice; add-selection-form.tsx renders notice, category made optional; new lib/parse-field-config.ts (extracted parsers to satisfy Stage-12 no-restricted-imports — plan.md's literal wording was wrong, this is the correct fix). Self-verified on own preview incl. null-guard against a real pre-Stage-22 project. Review-7 dispatched (diff-b5.patch).

- reviewer · B5 review round 1 (b1e82de vs release/stage-22, diff-b5.patch) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 4 MINOR. Report: `.engineering/stage-22/review-7.md`. Verified by running: `npm run lint` 0 errors, `npx tsc --noEmit` clean; the `no-restricted-imports` justification is real (eslint.config.mjs bans `@/lib/data/*` from `app/[orgSlug]/**`) and the parser extraction to `lib/parse-field-config.ts` is byte-identical to the removed bodies; `includeConfigSnapshot:true` exists in exactly one caller (project GET route) and B3's `omit` holds on all six other project row reads/writes; no `fetchProjectDetail` consumer spreads `project` into a client component and no client component fetches the detail route, so the raw snapshot never reaches the browser; snapshot rows are parsed via parseFieldsSchema/parseFieldOptionsConfig (review-6 carry-forward CLOSED) and `{}` vs `null` is normalised identically by `configurator-gating`'s `?? {}`; `componentTypes: []` keeps `usingSnapshot` true (no fallback, empty palette, per review-6); null-guard branch reproduces the pre-B5 live read incl. the 401/403 redirect; D-12 `createSelection` live-read guard unchanged. Review-5's `_project-fetch.ts`/GET-flag carry-forward CLOSED. MINORs: snapshot shape trusted without `Array.isArray` guard (500 instead of fallback on a malformed row — not reachable today); `listSelections`'s pre-existing `include: { componentType }` means the task file's literal "no componentType query" AC can't be ticked (display name only, palette freeze intact, but a ComponentType rename will show live in Saved Components — tester should not classify as a B5 bug); zero-active-types snapshot shows the misleading "ask an admin" empty state with no notice (task file puts empty snapshots out of scope); snapshot now rides the detail payload for all six pages under `/projects/[projectId]` (D-10 sanctioned, 3-30 KB real orgs; acme-glass's 82 KB is e2e litter, not representative). Notes: `by-page.sql` now owes B7 three batches' worth of Prisma shape changes (B3 omit + B4 snapshot read/write + B5 flag/conditional fetch); docs-repo `development-cycles/stage-22/` task folder is still untracked while the B5 task-format-retro note was committed (that note itself: in scope, docs-only, harmless).
- (orchestrator) CORRECTION: earlier merge of B5 into release/stage-22 silently failed (git switch aborted on an uncommitted worklog diff; subsequent merge/push then no-op'd on the wrong branch). B5's commits were never lost (feature/s22-b6 branched from the same point), but the release/stage-22 ref itself lagged. Fixed: release/stage-22 fast-forwarded to b1e82de and pushed; feature/s22-b6-design-submitted-at confirmed based on it. No B6 work was affected. Lesson for retro: always verify branch pointer with git log/merge-base after a merge, don't trust chained && output alone.

- developer · B6 clear `designSubmittedAt` -> branch `feature/s22-b6-design-submitted-at` (already checked out off release/stage-22), HEAD 775bdfd, pushed. CHANGED `lib/data/partitions.ts` `updatePartition` only: extended the existing tenancy-guard `findFirst`'s `include` (`room -> floor -> project`, `select: { designSubmittedAt: true }`) — reuses the already-loaded row per the task file, no extra query. After the existing `data`/`nextDesign` build-up (unchanged), added the D-14 check: if `existing.room.floor.project.designSubmittedAt !== null` AND the patch carries `design` and/or `heightMm` (geometry-affecting; label-only PATCHes skip this entirely), the partition update and a `prisma.project.update({ designSubmittedAt: null })` run inside one `prisma.$transaction([...])` (array form, per plan.md's literal wording) and the transaction's first result is returned; otherwise the original single `prisma.partition.update` runs unchanged, and when the flag is already null no project query is issued at all. No `ProjectCalculation` reference anywhere. No PATCH route change, no response-shape change (still `{ partition }`). Docstring updated to note the new behavior. Also added a docstring note that this is D-14's literal narrow scope (partition-PATCH-only clear). Docs: added two `backlog/backlog.md` entries (docs repo, separate commit) — (1) other post-submit-staling paths (Selection CRUD, Room.sides, Room/Partition/Floor deletes) don't clear the flag, deliberate stage-22 scope; (2) B1 review MINOR carry-forwards (DOOR ComponentType.code rename coupling; glass-swatch active-state cosmetic gap under full-height-door-only panels). `by-page.sql` untouched here — B6's only Prisma shape change is the `include` addition + a straightforward `project.update`, and plan.md assigns the by-page.sql reconciliation for all of B3/B4/B5/B6's accumulated shape changes to B7 (which also owns the stage's other docs). Verify: `npx tsc --noEmit` clean; `npm run lint` 0 errors (0 new warnings — the 6 pre-existing warnings are in unrelated e2e specs/wall-canvas.tsx, untouched by this diff). NOT done: preview/e2e verification (no browser tool in this dispatch; task file's 3 preview-based acceptance criteria — submit->edit->re-lock via UI, DB-row check after save, label-only PATCH leaves flag set — are unverified beyond code-reading). This is the last code batch; only B7 (tests/docs) remains. Status: DONE_WITH_CONCERNS (preview verification outstanding, same posture as prior batches at this point in their cycle).
- (orchestrator) B6 dev done (775bdfd code + 1d73433 worklog): updatePartition extends existing tenancy-guard include to pull project.designSubmittedAt, clears it in same $transaction only when submitted+geometry-changed patch. Backlog entries added (docs repo main, 231249a). Last code batch - only B7 remains. Dispatched in parallel: review-8 (diff-b6.patch) + devops preview verification (3 ACs: UI re-lock, DB row, label-only PATCH leaves flag).

- reviewer · B6 review round 1 (1d73433 vs release/stage-22, diff-b6.patch) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 5 MINOR. Report: `.engineering/stage-22/review-8.md`. All dispatch checks PASS (include reaches project.designSubmittedAt with no extra query; presence-of-design/heightMm condition matches D-14 at the API boundary; tenancy guard neither loosened nor duplicated; array `$transaction` atomic; fires only when flag non-null; no route/response change; no `ProjectCalculation` reference anywhere but a comment). Backlog rows read directly and verified accurate + in-format. Verified locally: tsc --noEmit clean, lint 0 errors. MINORs: every in-app Save bundles heightMm so a UI rename also clears (tester must use a direct API call for the label-only AC); presence-based not value-based clear; duplicate `projectId` const; TOCTOU note for Stage 23's extension of this tx; by-page.sql owes B6's project.update + include change to B7.

- devops · B6 preview verification (2026-09-20) — branch `feature/s22-b6-design-submitted-at` @ 1d73433, deployment `dpl_DsgLML2926U7Y1nAzSovY6XdRUjk` (READY, ~51s build), preview URL https://quotation-system-knkh55bm7-vistra-indias-projects.vercel.app. Health: 200 `{"status":"ok","database":"connected"}`. AC verification (all via direct API against preview, 15/15 assertions PASS): AC1 geometry PATCH (design-only and heightMm+design) clears designSubmittedAt in the same transaction — verified by GET project before+after; AC2 label-only `{ label: "..." }` PATCH (no design, no heightMm — per review-8 MINOR note) leaves designSubmittedAt unchanged — exact same timestamp before/after; AC3 geometry PATCH on unsubmitted project (null designSubmittedAt) returns 200, stays null, design persists. Reviewer note (AC2 must be direct API, not UI) confirmed. stage18.spec.ts: **14/14 PASSED** (25.5s). stage21-design.spec.ts: **7/7 PASSED** (54.4s). 3 throwaway projects cleaned up. Full report: `.engineering/stage-22/verify-b6.md`. **B6 CLEAR FOR MERGE.**

- developer · B7 tests/docs -> branch `feature/s22-b7-tests-docs` (off release/stage-22 @ 1d73433), pushed. Resolved all 8 open items from the Status block:
  (1)/(2) B2/B4 real `-- --write` runs: NOT executed (still gated on human go-ahead) — explicitly documented as pending in 04-data-model.md and the new stage-22.md Execution Log, with exact commands recorded, rather than silently left implicit.
  (3) stage19 step-gating failure: re-flagged in the exec log as pre-existing but noted the evidence is thin (no batch touches routing) — a fresh look at test phase recommended.
  (4) by-page.sql: updated all 5 areas the task file named — project list/detail explicit-omit column lists (replacing `SELECT p.*`), the snapshot-load read + `configSnapshot` column added to both `project.create` sites (createProject AND convertInquiryToProject — the task file only named the general "Project create" case), a new Configuration-page-fetch section documenting the snapshot-vs-live-read split, and the `designSubmittedAt` clear transaction added to the partition PATCH section (which also got a full v1->v2 rewrite — it was still describing the old panels[] shape).
  (5) transom-glass-source correction: `lib/partition-design.ts`'s doc comment (~L20, the one place still saying "v1 always had null there") fixed to state the human-approved order (panel's own selectionId -> defaults.glassSelectionId -> null) — NOTE: the B2 script's own header comment had ALREADY been corrected during B2 fix-round-1 (review-3), so only one file needed this fix, not two as the Status block assumed. `stage-22.md` Decision #2 and its "Migration from v1" prose corrected to the same order (docs repo).
  (6) npm script naming: `migrate:design-v2` already matched the task file exactly (no fix needed); `backfill:config-snapshot` (singular, the actual script name) did NOT match `batch-4-snapshot-capture.md`'s `backfill:config-snapshots` (plural) — fixed the task file, not the code (actual script name is the source of truth per dispatch instruction).
  (7) cell-id regeneration: carried into the new stage-22.md Execution Log's "Standing open items" section for Stage 23 visibility (was previously only in plan.md/worklog, not in the stage's own execution record).
  (8) feature branches: left as-is (human's call).
  NEW test code: `tests/unit/partition-design.test.ts` (U1-U6 adapted to `parseStoredDesign`/`parseDesignPatch` — the task doc's assumed `parseDesign()` was never built, D-3/D-17 — plus the door/transom round-trip and the transom-selectionId fallback-order invariant), `tests/unit/migrate-design-v1-to-v2.test.ts` (convertV1ToV2/classifyDesign/endpointOf), `tests/unit/config-snapshot.test.ts` (buildConfigSnapshot) — 32 tests total, run via `node --import tsx --test` (D-15; no new framework), `npm run test:unit` script added to package.json. `tests/e2e/db-helpers.ts` (new — guarded direct-Prisma DB helper for the two states no API can produce: a v1 design row, and a forced-NULL configSnapshot; dynamic `await import()` of the generated Prisma client, matching the B2/B4 scripts' pattern, because Playwright's CJS test loader can't statically `import` that ESM module — hit and fixed a `ReferenceError: exports is not defined` on first `--list` run). `tests/e2e/stage22-data-model.spec.ts` (new, API-level per the established stage18.spec.ts posture): E1/E2 (v1 row seeded via db-helpers, GET shows raw v1 doc, PATCH with a v2 body saves and the raw DB row confirms schemaVersion 2), E5/E8/E10 (new project's snapshot shape + tenancy + list-payload omission), E6 (freeze: a ComponentType added post-creation doesn't appear on an existing project's snapshot), E7 (null-guard via a forced-NULL DB write, detail route still 200s), E9 (designSubmittedAt clears on a geometry PATCH, not a label-only one). E3/E4 intentionally NOT duplicated — already covered by stage21-design.spec.ts's width-sum test and stage18.spec.ts's height-invariant/round-trip tests. Also fixed comment-only bug: `lib/partition-design.ts` line ~20. Also wrote the stage-22.md Execution Log (consolidates this file's activity log, batch by batch, plus the two standing open items) and flipped `04-data-model.md`'s `Partition.design` v2 / `Project.configSnapshot` status tags to ✅ Built with honest caveats about the pending B2/B4 real runs (not claiming 0 v1 rows or 100% backfill — that isn't true yet). Appended developer + tester note stubs are N/A (no tester dispatch in this turn); appended a developer retro entry to `task-format-retro.md` (docs repo).
  Verify: `npx tsc --noEmit` clean; `npm run lint` 0 errors (6 pre-existing warnings, none in touched files); `npm run test:unit` 32/32 pass; `npx playwright test --list` loads all 298 tests across 30 files cleanly (including the 5 new stage22 tests) — confirms the new spec/helper are syntactically and module-resolution correct, but the 5 new E2E tests themselves were **NOT run** against any live preview/environment (no browser-driving tool available in this dispatch, consistent with every other batch's posture at code-complete time) — that run is the tester's job at `engineering:test` against `test.easeetool.com`, or devops's job for an earlier preview-level check. Docs-repo changes (`by-page.sql`, `04-data-model.md`, `stage-22.md` Decision #2 + Execution Log, `batch-4-snapshot-capture.md` script-name fix, `task-format-retro.md`) committed separately in that repo. Status: DONE_WITH_CONCERNS (E2E tests written but unexecuted; B2/B4 real runs still pending human go-ahead — both concerns pre-existed this batch and are explicitly carried forward, not new).

- reviewer · backfill `--project` filter review round 2 (`feature/s22-backfill-single-project` @ ca607af vs 0269b1c, diff-backfill-filter-fix.patch — verified byte-identical to the real diff) -> verdict **APPROVE-WITH-NITS**; 0 CRITICAL / 0 IMPORTANT / 2 MINOR. Report: `.engineering/stage-22/review-11.md`. review-10's IMPORTANT confirmed CLOSED by measurement (10-case real `npm run` matrix in an isolated worktree against a bogus-but-allowlist-passing host; zero DB writes): the failure-mode form aborts pre-client with a clear message + exit 1, and all legitimate forms are unaffected. All five safety guards byte-identical to the baseline; both accepted MINORs (Backfilled:0 lines, count() scoped org-wide-only) landed. tsc + eslint clean. Cleared for the human-approved dev-DB run.

- (orchestrator) MERGE NOTE: this worklog append-conflicted because feature/s22-b7-tests-docs and feature/s22-backfill-single-project both branched off release/stage-22 @ 1d73433 and each independently appended entries as their own review rounds progressed. Resolved by keeping this side's entries (they already carry the sequence through to review-11's APPROVE-WITH-NITS) and folding in below the two entries unique to the backfill branch's copy (review-10's original CHANGES-NEEDED verdict and the fix-round developer entry) for anyone wanting the intermediate detail rather than just the final result. Also merged: the b3ab56b docs-only follow-up fixing review-11's MINOR 1/2 (a false claim that the guard also caught a misspelled `--projects=` flag).

- reviewer · backfill `--project=<id>` filter review (delta only, `feature/s22-backfill-single-project` @ 0269b1c vs release/stage-22) -> verdict **CHANGES-NEEDED**; 0 CRITICAL / 1 IMPORTANT / 3 MINOR. Report: `.engineering/stage-22/review-10.md`. Verified locally: tsc --noEmit clean, eslint clean, `parseProjectFlag` exercised over 10 mock-argv cases via tsx, npm 10.9.0 `--project` flag matrix re-measured, read-only dev-DB count probe (571 projects / 565 DbNull / 0 JsonNull; no writes). All five pre-existing safety guards diffed line-by-line against the pre-change file and confirmed unchanged; single-project path reuses the same conditional `updateMany`; no-flag path behaviourally identical. IMPORTANT is a fail-OPEN scope guard, not a data bug — fix before the human-approved `-- --write` run.
- (orchestrator) Backfill single-project filter review-10 = CHANGES-NEEDED (1 IMPORTANT: mistyped invocation e.g. `npm run backfill:config-snapshot --project=X -- --write` silently does a REAL ORG-WIDE write instead of scoping to X, because npm's swallowed `--project=X` leaves argv=["--write"] with no scope detected). Fix dispatched. NOT running anything against dev until fixed + re-reviewed.
- developer · review-10 fix round -> branch `feature/s22-backfill-single-project`, CHANGED `prisma/backfill-config-snapshots.ts` only. **IMPORTANT fixed**: after computing `projectFlag`, if `process.env.npm_config_project` is set and doesn't match `projectFlag.id` (i.e. npm swallowed `--project=<id>` because it was placed before `--`, or the flag reached npm's own config layer at all while argv disagrees), the script now aborts before the Prisma client is constructed with a message telling the user to put `--project=<id>` after `` `--` ``; mirrors the existing `npm_config_dry_run` belt-and-braces pattern per the review's suggested fix, placed in the same pre-client guard block. Does not attempt to catch the separate `--projects=` typo case (review flagged that as optional/narrower; noted in the header comment so it isn't mistaken for covered). **MINOR 3 fixed**: not-found and already-snapshotted branches now also print `Would backfill`/`Backfilled: 0` so the report shape is constant across modes. **MINOR 4 fixed**: `prisma.project.count()` now only runs in org-wide mode (`if (!targetProjectId)`); the `Projects total (org-wide):` report line moved into the org-wide branch only (single-project mode no longer prints a stale/zero org-wide total, addressing the report-labelled-oddly note under MINOR 3 too). **MINOR 2 (JSON-null-vs-DbNull) left alone** per dispatch — review confirmed it's currently unreachable (dev DB has 0 JsonNull rows) and no app path writes JSON null. Reused: existing `parseProjectFlag`/argv convention, existing `npm_config_dry_run` guard pattern (cited by the review), existing report `finally` block structure — no new helper module. Verify: `npx tsc --noEmit` clean, `npx eslint prisma/backfill-config-snapshots.ts` clean (0 problems); pure-logic mock check of the new guard (6 argv/env combinations reproducing the review's flag-matrix rows: correct placement after `--`, npm-swallowed-before-`--`, no flag, bare `--project`, env-matches-argv edge case) via a throwaway `tsx` script importing `parseProjectFlag` — all 6 PASS, script deleted after, never committed, no DB touched. NOT done: an actual dev-DB run (dry or real) — out of scope for this fix, unchanged from before. Status: DONE.

- developer · docs-only follow-up (`b3ab56b` on feature/s22-backfill-single-project): corrected the header comment (and the inline guard comment) that falsely claimed the npm-swallowed-`--project` abort also catches a misspelled `--projects=` flag — it does not (npm derives a different env var name from the flag as typed), per review-11.md MINOR 1/2. tsc/eslint clean. No behavior change.
- (orchestrator) Merged feature/s22-backfill-single-project into release/stage-22 (non-ff, branches had diverged after B7) alongside feature/s22-b7-tests-docs (already merged ff-only earlier). All Stage 22 implement-phase work (B1-B7 + the backfill filter follow-up) is now on release/stage-22. Remaining before handoff to engineering:test: the two human-gated real `-- --write` runs above, and naming a target project for the single-project backfill test.

- devops · B4 backfill single-project run (2026-09-20) — branch release/stage-22 @ 993e64b (clean), dev DB endpoint `ep-dark-term-ai0ufj4k` (confirmed in both .env and .env.local; production endpoint absent). Target project: `449ec773-864b-4829-a4d4-da5c12635de0` (org `vistra` / `ab23d299-d4f3-4cb1-a349-ac0a2caba1cc`). Before-state: `configSnapshot IS NULL` = true; global non-null count = 6. **Dry run** (`npm run backfill:config-snapshot -- --project=449ec773-864b-4829-a4d4-da5c12635de0`): exit 0, Would backfill: 1, scope restricted to this project, 0 failures. **Real write** (`-- --write`): exit 0, Backfilled: 1, 0 failures. After-state SQL checks: `has_snapshot=true`, `takenAt="2026-09-20T06:07:31.654Z"` (fresh ISO), `ct_count=3`, snapshot keys `{takenAt, componentTypes}` only, snapshot IDs/codes match vistra org's live ComponentTypes exactly (DOOR/GLASS/PROFILE_STOP — IDs byte-for-byte; no cross-org data; organizationId intentionally absent from snapshot shape per B4 design), global non-null count = 7 (+1 exactly). Full report: `.engineering/stage-22/backfill-run-449ec773.md`. **Safe to proceed with the full-org-wide `-- --write` run when human gives the go-ahead.**

---

## 2026-09-20 — Door panel "unselected dropdowns" investigation

**Task:** Determine whether the "Select" placeholder on Door Type and Glass Variant in the "Edit Door" panel for selection `e2e-stage21-domlabels-selection-1789847476812` (project `449ec773-864b-4829-a4d4-da5c12635de0`, org vistra) is a real UI pre-select bug or expected behavior.

**Result:** EXPECTED — pre-existing e2e test litter. No bug.

**Key facts (read-only SQL against dev DB `ep-dark-term-ai0ufj4k` confirmed):**
- `Selection.config` for this row is `{}` — completely empty. No `doorType`, no `glassVariant`, no field values at all.
- Label `e2e-stage21-domlabels-{timestamp}` is unambiguous Stage 21 e2e DOM-label test fixture; the test created the Selection to exercise label-text rendering only, never filling form values.
- This is the only Selection in the project; the entire project is test litter.
- `handleEditSelection` in `add-selection-form.tsx` initializes `fieldValues` from `sel.config`. With `config: {}`, it sets `fieldValues = {}` — the "Select" placeholder is correct behavior.
- configSnapshot backfill was verified intact: DOOR entry has `fieldOptionsConfig.doorType.options` (3 items) and `fieldOptionsConfig.glassVariant.options` (2 items), explaining why dropdowns DO populate on click.

**Findings file:** `.engineering/stage-22/investigate-door-select.md`

- devops · B4 backfill org-wide run (2026-09-20) — branch release/stage-22 @ 993e64b (clean), dev DB endpoint `ep-dark-term-ai0ufj4k` (confirmed; production endpoint absent). **Dry run** (org-wide, no --write): 571 total projects, 564 NULL configSnapshot, 0 failures, 7 already had snapshot (includes the 449ec773 single-project run). Count 564 vs prior 563 is expected drift — ballpark sane. **Real write** (`-- --write`, human-authorized): ran ~12 min (12:08–12:20 IST); Backfilled: 564, 0 failures, exit 0. **SQL verification** (read-only Prisma script): 571/571 NOT NULL, 0 NULL; 4 distinct orgs covered (ab23d299/vistra 87, f74551ab/acme-glass 460, 38cf3f25 4, f024afd5 20); snapshot shape `{takenAt, componentTypes: [{id, code, name, active, fieldsSchema, fieldOptionsConfig}]}` correct; tenancy verified for all 3 checked orgs (snapshot CT ids match live org exactly). **Idempotency dry run**: Would backfill: 0 — confirmed script is safe to leave in repo. **0 failures throughout.** Full report: `.engineering/stage-22/backfill-run-orgwide.md`. Dev DB is now fully backfilled; 0 projects remain with NULL configSnapshot.

- tester · Stage 22 test pass round 1 (target test.easeetool.com @ 676c529) -> verdict **PASS**; 0 CRITICAL / 0 HIGH / 1 MEDIUM (snapshot-vs-live Selection guard question) / 3 LOW. Report: `.engineering/stage-22/bugs-1.md`. Tests committed on `feature/s22-test-coverage` @ ef9f784 (pushed, not merged). Full Playwright suite: failures triaged as pre-existing/stale/flaky (stage6/7/14/19), none Stage-22. Test data cleaned up.

- developer · test-round-1 fix batch (bugs-1.md M-1/L-1/L-2; L-3 dropped) -> branch `feature/s22-fix-bugs-1` (off release/stage-22 11ee729 + merged feature/s22-test-coverage ef9f784). M-1: `lib/data/selections.ts` createSelection validates active + isComponentTypeFullyConfigured against `project.configSnapshot` (live fallback only on NULL snapshot; live row still checked for tenancy); L-1: `MAX_DESIGN_MM=100000` in `lib/partition-design.ts` (positiveInt + running width sum), partitions/[id] route heightMm integer <=100000; L-2: comment fix in projects/[projectId]/route.ts. New e2e test in stage22-test-coverage.spec.ts (M-1, both directions). Docs: decision #11, B2 status, test-round-1 line, by-page.sql (docs repo eab7bf9). Patch: diff-fix-bugs-1.patch. Status: see return message for verification results.
  - verify: lint 0 errors, tsc clean; preview https://quotation-system-8xpl796jn-vistra-indias-projects.vercel.app @ 59f1734: stage22-test-coverage 10/10 (incl. new M-1 + overflow), configurator-gating green (spec updated: post-config project, decision #11), stage22-data-model 5/5, stage18+stage21-design green. Status: DONE.

- reviewer · review-13 (fix-bugs-1) · APPROVE-WITH-NITS · 0 critical / 0 important / 1 minor (+1 note) · see review-13.md

- tester · Stage 22 re-test round 2 (test.easeetool.com @ caaf732 / release 0374676) -> **PASS**; 0 CRITICAL / 0 MAJOR / 0 MINOR new. M-1, L-1, L-2 verified resolved (edge cases incl. cap 100000/100001, deleted-type-in-snapshot, cross-org). Regression: lint/tsc/unit clean; full Playwright 211 pass/12 fail under load, 6 fail at workers=1, all pre-existing (stage6/7/14, stage19 summary test stale re designSubmittedAt gate, 2 flaky). New specs: feature/s22-test-round2 @ 3316e0e. Report: `.engineering/stage-22/bugs-2.md`.

- devops · Deploy step 1 (2026-09-20) — merged feature/s22-test-round2 -> release/stage-22 (8fb5587) -> staging (49cef18) via temp worktree (removed). Staging builds `dpl_GLTfadXahL5hGhQkSi64jApAYLr2` and retrigger 95f2ebd `dpl_GthCrVnQ42ZuDjJP9scziMMKmaHY` both ERROR P1002 (migrate deploy). Verified on dev: advisory lock 72707369 held (granted) by idle pooled backend pid 1115 (~58 min). test.easeetool.com still serves prior READY build caaf732: /api/health 200 {"status":"ok","database":"connected"}. Build-log route list NOT verified. Migrations none applied this step. Prod recon: prod DATABASE_URL is Sensitive (not pullable), neonctl needs browser auth -> items 1-3 blocked; read-only script at .engineering/stage-22/prod-recon-readonly.ts (smoke-tested on dev). Findings + recommended prod plan: .engineering/stage-22/deploy-recon.md. Status NEEDS_ARCHITECT. No prod writes, master untouched.

- architect · Mode C consult 2026-09-20 (recorded by devops) · rulings: root cause of staging P1002 is `migrate deploy` through the pooler (session advisory lock across pooled backends). Fix = override DATABASE_URL with DATABASE_URL_UNPOOLED for the migrate step only in scripts/build.mjs; prisma.config.ts unchanged. B2 must run strictly AFTER new code is live on prod; B4 after migrate. Backup = Neon branch of main + in-DB `_PartitionDesignV1Backup` table. Prod credentials stay with the human. Dev password exposure -> backlog (Low, with drop-backup-table item). Docs: stage-22.md D-2 amended, D-18, D-19 (quotation-system-docs commit pushed).

- devops · Deploy steps 0-4 (2026-09-20) · DONE. Docs commit fb06da7 (quotation-system-docs, pushed): stage-22.md promote-alone note, D-2 amended, D-18 (unpooled migrate), D-19 (EXPECT_ENDPOINT guard: decided, NOT yet implemented in B2/B4 scripts); backlog +2 Low. profile.md corrected (stale "P1002 transient" note, promotion plan). DATABASE_URL_UNPOOLED key present in Vercel Preview+Production. feature/s22-migrate-unpooled 80a5005 -> release/stage-22 c55f727 -> staging a8dd724. Staging `dpl_9kL1BN7dgyqQuKWdFEXFv6m8GuZf` READY first try, migrate clean, routes present, test.easeetool.com /api/health 200 database connected. No DB writes; master untouched. See deploy-recon.md "Update 2026-09-20 (later)".

- tester · Stage 22 re-test round 2 CORRECTED (test.easeetool.com @ a8dd724, includes fix batch + round2 specs + migrate-unpooled) · verdict **PASS** · 0 CRITICAL / 0 MAJOR / 0 MINOR · M-1, L-1, L-2 resolved; edge cases confirmed (cap 100000/100001, deleted-in-snapshot 400 not 500, cross-org 400/404, deactivated after snapshot 201, added after project 400); no regressions in any Stage-22 suite (stage22-data-model 5/5, stage22-test-coverage 10/10, stage22-round2 2/2, stage18 14/14, stage21-design 7/7, configurator-gating green); full Playwright 225 pass / 10 fail / 27 skip (all 10 pre-existing/flaky); lint 0 errors, tsc clean, unit 32/32; test data cleaned. NOTE: line-142 entry superseded (numbers 211/12 were from an interrupted prior attempt). Report: `.engineering/stage-22/bugs-2.md`.

- devops · Deploy / production promotion (2026-09-20) — `staging` -> `master` fast-forward, master `cf130caae01939b624de7ea3c482650ebb153116`, deployment `dpl_D5Ug1mwrkRhUheejjXCKSrC82d58` READY. Migration `20260919000001_add_project_config_snapshot` applied cleanly in the build (unpooled URL, no P1002). `easeetool.com/api/health` 200, database connected. Prod recon before: 4 orgs, 2 projects, 3 rooms, 2 partitions v1, 0 malformed/0 rejects. B2 run on prod by the human (post-run recon v1:0, v2:2, 0 violations). B4 backfill run by the human (USER-REPORTED "all done"; post-B4 recon not shown to an agent, not independently verified). Backup table `_PartitionDesignV1Backup` (2 rows) on prod, drop after soak (backlog item exists). No Neon snapshot branch (human decision: tiny data). Post-deploy `engineering:regression` SKIPPED by the human; human verified production manually. Stage 22 promoted alone (D-2 amended, D-18, D-19). Incidents: P1002 via pooler (fixed D-18); recon script env var (fixed); dev DB password exposed in output (backlog: rotate); human's checkout on older branch without the guard -> clean worktree `quotation-system-prod-run`. Stage closed; docs commit 047d797 (quotation-system-docs, pushed).
