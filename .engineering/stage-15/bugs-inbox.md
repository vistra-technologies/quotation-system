# Stage 15 — Bug inbox (raw, pre-scoping)

**Source:** `Bugs and Issues.docx` (human-authored, 2026-08-13, 11 screenshots)
**Extracted:** 2026-08-14 during `engineering:stage-prep`
**Status:** RAW — not yet scoped or approved. IDs are stable; use them in discussion.

Environment shown in screenshots: `vistra.test.easeetool.com` (staging, Stage 13+14).

---

## A. Dashboard

| ID | Item | Screenshot |
|---|---|---|
| **D1** | `Welcome, vistra` shows the **org slug**, not the user. Should be `Welcome, <User Full Name>` (e.g. "Admin User"). | image5 |
| **D2** | Profile dropdown shows `vistra admin`. Should show the **username** instead. | image5 |
| **D3** | The three KPI cards (Orders / Projects / Inquiries) show **the same counts for everyone**. Should be: Admin + Company Member → org-wide totals; **all other roles → only their own records**. | image9 |
| **D4** | The org / external-company pills (`Vistra Partitions`, `Vistra Partitions Dist Co`) sit top-left. Move them to the **right corner, beside the dashboard/home button**. | image1 |

## B. Inquiry List

| ID | Item | Screenshot |
|---|---|---|
| **L1** | **Pagination is broken.** 11 inquiries all render on one page instead of breaking at 10. The numbered page buttons (1, 2, 3, 4 …) are missing from the footer — they should grow as pages are added. | image8 |
| **L2** | Add a **Created By** column: show the **username**; on hover show the **full name**. (e.g. `abc123` → "ABC Bajaj") | image8 |
| **L3** | Switching **My Inquiries ↔ All Inquiries** fetches data with **no loading overlay**. Add one here **and everywhere else that performs a loading operation**. | image8 |

## C. Inquiry Create

| ID | Item | Screenshot |
|---|---|---|
| **C1** | `CLIENT (OPTIONAL)` → rename to **`Company`**, drop the "(optional)" suffix. | image10 |
| **C2** | **Row 1 restructure:** Inquiry Id on the left (replacing the bare `#`), Company on the right. | image10 |
| **C3** | Inquiry Id field is disabled; on hover show tooltip **"Inquiry Id will be autogenerated"**. | image10 |
| **C4** | **Row 2:** Project Name (renamed from "Inquiry Name") left, Project Location right. | image10 |
| **C5** | **Row 3:** Currency left, Amount right. **Live-format the amount as it is typed** — `00,00,000` (Indian grouping) for INR *and* AED; `000,000` (Western grouping) for USD. | image10 |
| **C6** | Currency defaults to the External Company's default currency, else INR. *(May already work — verify, don't assume.)* | image10 |
| **C7** | **Row 4:** Submission Date + Project Deadline. | image10 |
| **C8** | Remove the `e.g. …` example hints from **all** fields **except Project Location**. | image10 |
| **C9** | Add validation / input restrictions: **Project Budget** → numbers only; **Project Name, contractor & consultant details, end-client details** → alphanumeric + spaces + hyphens. | — |
| **C10** | **Address Line 2** → make optional. | — |

## D. Inquiry View

| ID | Item | Screenshot |
|---|---|---|
| **V1** | Header sub-line shows too much (`India`, `INR`, status, company, created date, user). Reduce to **Company name + Status** only. | image11 |
| **V2** | The **right side of the header is empty** — put **Username + Created Date** there as metadata. | image11 |
| **V3** | **Destination Country** appears in Project Information but **is not a field on the form**. | image11 |
| **V4** | `CLIENT` label → rename to **`Company`**. | image11 |
| **V5** | **Project Budget renders raw** (`1000000`) — apply the same currency formatting as C5. | image11 |

## E. Project List

| ID | Item |
|---|---|
| **PL1** | **Same fixes as the Inquiry list** — pagination (L1), Created By column (L2), loading overlay (L3). |

## F. Project Create

| ID | Item |
|---|---|
| **PC1** | **Same rules as Inquiry Create** (C1–C10), replicated. |
| **PC2** | Additionally: **Project Id** replaces the bare `#`. Rest of the structure is identical. |

## G. Project View

| ID | Item | Screenshot |
|---|---|---|
| **PV1** | The **`1 Project Details` wizard pill is not highlighted** while on the Project Details page. Same problem on **all subsequent wizard steps**. | image6 |

## H. User Management (list)

| ID | Item | Screenshot |
|---|---|---|
| **U1** | Row action icons (pencil / trash) **look bad** — replace with proper icons. | image4 |
| **U2** | Put the row action icons **inside boxes so they read as buttons** (reference: image7 — a filled, rounded-square button). Apply the **same treatment to every edit/delete row action across all list pages**. | image4, image7 |

## I. New User

| ID | Item | Screenshot |
|---|---|---|
| **U3** | **External Company must be conditional on Role.** Only **Admin** and **Company Member** may leave it empty; **every other role requires a company**. | image2 |

## J. Edit User

| ID | Item | Screenshot |
|---|---|---|
| **U4** | **No way to edit** First Name, Last Name, Mobile, Email, or External Company. | image3 |
| **U5** | Page needs a **redesign** — currently a long scroll of stacked cards. No mockup exists; design to fit the area, consistent with the Sage Ease theme. | image3 |

## K. Permissions / seed data

| ID | Item |
|---|---|
| **U6** | Test-generated permissions (`E2E_PERM_1784059852197` and others) are **visible in the Permissions admin UI**. Either (a) consolidate into one testing permission + testing role + testing user with full access, or (b) remove them entirely if unneeded. **Internal test artifacts must not be visible here.** |

---

## L. Carried forward (not from the docx — parked from Stages 12/14)

| ID | Item |
|---|---|
| **X1** | Project **edit** form shows `—` for Project Number; the inquiry edit form shows the real number. `projectNumber` missing from `EditProjectFormProps`. (Stage 14 MINOR-1) |
| **X2** | `.replace("Projects", "Project")` string surgery on the localized back-link in `projects/[projectId]/edit/page.tsx`. (Stage 14 MINOR-2) |
| **X3** | Mockups contain inline-validation markup (12 `field-error` spans, `form-error-banner`, `emailError`) that exists **nowhere in code** — build it or strip it from the mockups. Overlaps **C9**. |
| **X4** | Naming mismatch: "Contractor Details" vs "Contractor & Consultant Details". |
| **X5** | `admin-stage4.spec.ts:184` tenancy assertion was weakened during the Stage 14 spec repair — a two-slug **denylist** instead of a positive prefix. A leak to a third org would pass. |
| **X6** | Two ~1-in-15 E2E flakes on full serial runs: `stage6.spec.ts:379` and the shared `signIn` helper (`helpers.ts:34`). |
| **X7** | Two MINOR follow-ups logged in `stage-12.md`. |

---

**Count:** 30 items from the docx + 7 carried forward = **37**.
Features are explicitly deferred — the human will add them to this stage separately.
