# Stage 15 — Batch D record

**Role:** developer
**Branch:** `feature/stage15-form-layout`
**Final commit SHA:** `65d8f03`
**Preview URL:** `https://quotation-system-8vo5rj4lv-vistra-indias-projects.vercel.app`
**Status:** DONE

---

## Implementation plan

### Files changed

**`messages/en.json`**:
- `inquiries.fieldExternalCompany`: "Client (optional)" → "Company"
- `inquiries.fieldExternalCompanyReadOnly`: "Client (read-only)" → "Company"
- `inquiries.fieldName`: "Inquiry Name" → "Project Name"
- `inquiries.fieldId`: (new key) "Inquiry Id"
- `projects.fieldExternalCompany`: "Client (optional)" → "Company"
- `projects.fieldId`: (new key) "Project Id"
- Note: `colNumber` ("#") NOT changed — used by list column headers. New `fieldId` keys carry the label for form use only.

**`app/[orgSlug]/inquiries/new/create-inquiry-form.tsx`** (C1, C2, C3, C4, C7, C8):
- Company field moved from full-width above-grid into grid Row 1 right
- Id field in Row 1 left, labeled `t("fieldId")` = "Inquiry Id"
- `title="Inquiry Id will be autogenerated"` on the disabled Id input
- Grid reordered: Row 1 [Id, Company], Row 2 [Project Name, Project Location], Row 3 [Project Budget, Currency], Row 4 [Submission Date, Project Deadline]
- All `placeholder="e.g. …"` removed except `projectLocation`

**`app/[orgSlug]/inquiries/[inquiryId]/edit/edit-inquiry-form.tsx`** (C1, C2, C4, C7, C8):
- Company (read-only) moved from above-grid into grid Row 1 right
- Id field (showing real inquiry number) in Row 1 left, labeled `t("fieldId")`
- No tooltip on edit form (real number shown, not "—")
- Grid reordered identically to create form
- All `placeholder="e.g. …"` removed except `projectLocation`

**`app/[orgSlug]/projects/new/create-project-form.tsx`** (PC1/C1, PC2/C2, C3, C4, C7, C8):
- Company field moved from above-grid into grid Row 1 right
- Id field in Row 1 left, labeled `t("fieldId")` = "Project Id"
- `title="Project Id will be autogenerated"` on the disabled Id input
- Inquiry No. field removed (always "—" on direct project creation; adds no UX value)
- Grid reordered identically to inquiry create form
- All `placeholder="e.g. …"` removed except `projectLocation`

**`app/[orgSlug]/projects/[projectId]/edit/edit-project-form.tsx`** (PC1/C1, PC2/C2, C4, C7, C8):
- Company (read-only) moved from above-grid into grid Row 1 right
- Id field (real project number) in Row 1 left, labeled `t("fieldId")`
- Inquiry No. field removed (consistent with create form; project detail page shows linked inquiry)
- `inquiryNumber` removed from destructuring (prop still in interface for backward compat)
- Grid reordered identically; Submission Date + Deadline already together, preserved
- `placeholder="e.g. 250,000"` removed from projectBudget (only remaining e.g. on this form)

### SQL mirror conclusion

No `prisma.<model>.<method>` call shapes changed. This batch is labels and layout only. No `by-page.sql` update required.

---

## Verification table — per form × per item

All checks run against `https://quotation-system-8vo5rj4lv-vistra-indias-projects.vercel.app` via Playwright.

| Item | create-inquiry-form | edit-inquiry-form | create-project-form | edit-project-form |
|---|---|---|---|---|
| **C1/PC1 — "Company" label** | ✓ "Company" in label list; 0 "Client (optional)" occurrences | ✓ "Company" in label list; 0 old "client (read-only)" occurrences | ✓ "Company" in label list; 0 "Client (optional)" | ✓ 0 old client label occurrences |
| **C2/PC2 — Row 1: [Id, Company]** | ✓ label order: "Inquiry Id", "Company", "Project Name *", "Project Location *" confirms Row 1=[Id,Company] | ✓ "Inquiry Id","Company","S14-India-Co…" confirms layout | ✓ label order: "Project Id","Company","Project Name*","Project Location*" | ✓ "Project Id","—"(company value),"Project Name*","Project Location*" |
| **C3 — tooltip on disabled Id (create only)** | ✓ `title="Inquiry Id will be autogenerated"` on disabled input with value "—" | N/A — real number shown (INQ-1); no tooltip appropriate | ✓ `title="Project Id will be autogenerated"` on disabled input | N/A — real number shown (#101); no tooltip |
| **C4 — "Project Name" label + row layout** | ✓ "Project Name *" in label list; "Project Location *" follows immediately (same row) | ✓ "Project Name *", "Project Location *" consecutive | ✓ "Project Name*", "Project Location*" consecutive | ✓ "Project Name*", "Project Location*" consecutive |
| **C7 — Submission Date + Deadline same row** | ✓ "Submission Date *" and "Project Deadline" are adjacent in label sequence (Row 4 of 2-col grid) | ✓ same | ✓ same | ✓ same |
| **C8 — no e.g. placeholders except Location** | ✓ e.g. placeholder fields = ["projectLocation"] only | ✓ e.g. placeholder fields = ["projectLocation"] only | ✓ e.g. placeholder fields = ["projectLocation"] only | ✓ e.g. placeholder fields = ["projectLocation"] only |

### Verification evidence (raw output from Playwright)

**create-inquiry-form** labels (first 15): `["Inquiry Id","Company","Project Name *","Project Location *","Project Budget","Currency *","Submission Date *","Project Deadline","Main Contractor","Interior Contractor","Main Consultant","Interior Consultant","End Client Name *","End Client Phone *","End Client Email *"]`
- disabled[0]: title="Inquiry Id will be autogenerated", value="—"
- "client" occurrences: 5 (all from "End Client Name/Phone/Email/Address/Details" section — correct)

**edit-inquiry-form** labels (first 20 inc. p tags): `["Admin","Update the details for this inquiry.","Core details about this inquiry and its project","Inquiry Id","Company","S14-India-Co-1786738058778","Project Name *","Project Location *","Project Budget","Currency *","Submission Date *","Project Deadline","...","Main Contractor",...]`
- old client label count: 0
- disabled[0]: title=null, value="INQ-1" (real number, no tooltip)
- e.g. fields: ["projectLocation"]

**create-project-form** labels: `["Project Id","Company","Project Name*","Project Location*","Project Budget","Currency*","Submission Date*","Project Deadline","Main Contractor","Interior Contractor","Main Consultant","Interior Consultant"]`
- disabled[0]: title="Project Id will be autogenerated", value="—"
- old "Client (optional)" count: 0
- e.g. fields: ["projectLocation"]

**edit-project-form** labels: `["Admin","Update the details for this project.","Project Information","Core details about this project","Project Id","—","Project Name*","Project Location*","Project Budget","Currency*","Submission Date*","Project Deadline","Contractor & Consultant Details",...]`
- old client label count: 0
- disabled[0]: title=null, value="#101" (real number)
- e.g. fields: ["projectLocation"]

---

## Regression check

`stage15.spec.ts` (Batch A): **3 passed (42.3s)** against deployment `k8v74nv5z` (review round 1 build).

---

## Review round 1 — CHANGES-NEEDED finding and resolution

**Finding:** Removing Inquiry No. from the project **edit** form was wrong. The field was genuinely inert on the **create** form (always "—"), but on the edit form `page.tsx` computes `formattedInquiryNumber` from `project.inquiry` (non-null for projects converted from inquiries via "Start Project") and passes it as `inquiryNumber`. My change left the prop declared in `EditProjectFormProps` and computed/passed from `page.tsx`, but the component ignored it — dead code with a real data loss.

**Human decision (final):**
1. Restore the field on the **edit** form only, as a full-width `sm:col-span-2` read-only row placed directly below the `[Project Id | Company]` row.
2. Keep it removed from the **create** form (genuinely inert there — `projects/new/page.tsx` never computed or passed `inquiryNumber`).

**Fix (commit `d9a763f`):**
- Re-added `inquiryNumber` to the destructuring in `EditProjectForm`
- Added `<div className={\`\${fieldCls} sm:col-span-2\`}>` with a disabled input showing `inquiryNumber ?? "—"` and label "Inquiry No.", placed immediately after the `[Project Id | Company]` row in the grid

**Verification of the restored field (deployment `k8v74nv5z`):**
- Found project `6bcaa824` linked to inquiry #227 (DRAFT status, inquiry.inquiryNumber=227, companyInquiryNumber=null → renders `#227`)
- Edit form disabled inputs: `["#96" (Project Id, normal-width), "#227" (Inquiry No., col-span)]`
- `Inquiry No.` label confirmed; value `#227` confirmed; full-width placement confirmed via `col-span` class detection
- Non-inquiry-linked projects: Inquiry No. row shows `"—"` (correct fallback)
- Create form: Inquiry No. row absent (correct — was genuinely removed, not restored)

---

## What was reused

- `t("fieldExternalCompanyNone")` / `CompanyDropdown` patterns — unchanged from existing code; Company element moved but not rebuilt.
- `todayLocal` pattern — unchanged; moved inside the grid.
- `inputCls`, `selectCls`, `labelCls`, `fieldCls` class strings — unchanged; same idiom throughout.

---

## Decisions taken

- **Inquiry No. removed from project create/edit forms**: Always "—" on create (no UX value); on edit, project detail page already shows the linked inquiry. Consistent with the target 4-row layout [Id, Company / Name, Location / Budget, Currency / Date, Deadline].
- **No tooltip on edit forms** for C3: The tooltip "will be autogenerated" is misleading when the real number is already shown. Added only to create forms.
- **`fieldExternalCompanyReadOnly` updated to "Company"** (was "Client (read-only)"): The C1 rename was for "Client" → "Company". Even though the spec only said "update if it says (optional)", leaving "Client" in the label contradicts the intent of the rename.
- **No new automated tests**: All items in this batch are labels/layout. Testing posture is wireframe-stage; manual verification done and documented above.
